# DISABLED — PDF generation requires Redis and system-level dependencies
# that are complex to configure in CI. PDFs are built locally and committed.
name: Strategic PDF Generation (DISABLED)
on:
  workflow_dispatch: {}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PDF builds are performed locally. Run pnpm vault:sync to rebuild."

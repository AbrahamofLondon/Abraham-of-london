# DISABLED — This workflow has unresolved configuration issues:
# - Uses npm instead of pnpm (project standard)
# - References Vercel secrets not configured
# - Health check domain incorrect
# Re-enable after configuring Vercel integration.
name: Deploy Predictive Intelligence (DISABLED)
on:
  workflow_dispatch: {}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See file header for details."

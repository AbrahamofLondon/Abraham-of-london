// Server wrapper for contextual-efficacy page
// Auth and data loading handled by parent layout (app/admin/layout.tsx)
import AdminContextualEfficacyPageClient from "./PageClient";

export default function AdminContextualEfficacyPage() {
  return <AdminContextualEfficacyPageClient />;
}

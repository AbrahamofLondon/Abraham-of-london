// Server wrapper for content page
// Auth and data loading handled by parent layout (app/admin/layout.tsx)
import ContentRedTeamPageClient from "./PageClient";

export default function ContentRedTeamPage() {
  return <ContentRedTeamPageClient />;
}

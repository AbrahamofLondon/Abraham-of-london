// Server wrapper for strategy-room page
// Auth and data loading handled by parent layout (app/admin/layout.tsx)
import StrategyRoomSimulatorPageClient from "./PageClient";

export default function StrategyRoomSimulatorPage() {
  return <StrategyRoomSimulatorPageClient />;
}

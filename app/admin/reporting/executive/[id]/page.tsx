// app/admin/reporting/executive/[id]/page.tsx
// Legacy executive report ID route.
// Redirect handling is defined in next.config.mjs.
// This page should never render in normal operation.

export const metadata = {
  robots: "noindex, nofollow",
};

export default function LegacyExecutiveReportIdPage() {
  return null;
}
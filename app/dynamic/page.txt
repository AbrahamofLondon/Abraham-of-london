// src/app/dynamic/page.tsx
import React from 'react';
import LiveStockTracker from '@/components/LiveStockTracker';
import PortfolioManager from '@/components/PortfolioManager';
import StockCharts from '@/components/StockCharts';
import { fetchRealtimeStock, getMultipleStocks } from '@/lib/stock-service';

export const dynamic = 'force-dynamic';
export const revalidate = 0;

const DEFAULT_SYMBOLS = ['TSLA', 'AAPL', 'MSFT', 'GOOGL', 'AMZN'];

export default async function AdvancedStockPage() {
  // Fetch initial data for multiple stocks
  const stocksData = await getMultipleStocks(DEFAULT_SYMBOLS);
  
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50 py-8 px-4">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="text-center mb-12">
          <h1 className="text-5xl font-bold text-gray-900 mb-4 bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
            Live Market Dashboard
          </h1>
          <p className="text-xl text-gray-600 max-w-3xl mx-auto">
            Real-time stock tracking with advanced analytics, portfolio management, and interactive charts
          </p>
          <div className="flex justify-center gap-4 mt-6">
            <div className="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-medium">
              Live Data
            </div>
            <div className="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-medium">
              Auto-Refresh
            </div>
            <div className="bg-purple-100 text-purple-800 px-3 py-1 rounded-full text-sm font-medium">
              WebSocket Ready
            </div>
          </div>
        </div>

        {/* Main Dashboard Grid */}
        <div className="grid grid-cols-1 xl:grid-cols-3 gap-8 mb-12">
          {/* Live Stock Tracker - Full width on mobile, 2/3 on desktop */}
          <div className="xl:col-span-2">
            <LiveStockTracker initialStocks={stocksData} />
          </div>
          
          {/* Portfolio Manager - Full width on mobile, 1/3 on desktop */}
          <div className="xl:col-span-1">
            <PortfolioManager />
          </div>
        </div>

        {/* Charts Section */}
        <div className="mb-12">
          <StockCharts symbols={DEFAULT_SYMBOLS} />
        </div>

        {/* Market Overview */}
        <div className="bg-white rounded-2xl shadow-2xl border border-gray-200 p-8">
          <h2 className="text-3xl font-bold text-gray-900 mb-6">Market Overview</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <MarketMetric 
              title="Total Tracked Stocks" 
              value={DEFAULT_SYMBOLS.length.toString()} 
              change="+2 this week" 
            />
            <MarketMetric 
              title="Average Return" 
              value="+2.4%" 
              change="+0.8% today" 
              positive 
            />
            <MarketMetric 
              title="Market Cap" 
              value="$12.4T" 
              change="+$240B today" 
            />
            <MarketMetric 
              title="Active Sessions" 
              value="1,247" 
              change="Live" 
            />
          </div>
        </div>
      </div>
    </div>
  );
}

function MarketMetric({ title, value, change, positive }: { 
  title: string; 
  value: string; 
  change: string; 
  positive?: boolean;
}) {
  return (
    <div className="text-center p-6 bg-gray-50 rounded-xl hover:bg-gray-100 transition-colors">
      <h3 className="text-sm font-semibold text-gray-500 mb-2">{title}</h3>
      <p className="text-2xl font-bold text-gray-900 mb-1">{value}</p>
      <p className={`text-sm ${positive ? 'text-green-600' : 'text-gray-500'}`}>
        {change}
      </p>
    </div>
  );
}
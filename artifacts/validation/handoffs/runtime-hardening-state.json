{
  "schemaVersion": "runtime-hardening.handoff.v4",
  "generatedAt": "2026-07-08T12:20:00Z",
  "canonicalBranch": "construction/estate-restoration",
  "canonicalHead": "bc7617284",
  "worktreeClean": true,
  "executedThisSession": {
    "migrationReconciliation": "DONE + PROVEN on Postgres 15.18 — squashed 00000000000000_baseline (221 tables), 54 migrations archived to prisma/_archived-migrations-pre-baseline (reversible). Fresh migrate deploy exit 0; existing-DB resolve→deploy no-op exit 0.",
    "fullSuite": "GREEN at 167910651: 493 files / 7448 tests / 0 failures (timeout fix confirmed).",
    "pilotProductionPersistence": "DONE + PROVEN — OperatorPilotIntake + OperatorPilotTransition models + migration 20260708105750; Prisma adapter (history + human-authority + optimistic concurrency) proven against real Postgres; DB-free shared module (no better-sqlite3 in prod bundle); composed store (prod→Prisma, test→SQLite via dynamic import); both pilot routes rewired to async composed store.",
    "goForwardMigrationWorkflow": "PROVEN — prisma migrate dev creates+applies new migrations on the reconciled baseline."
  },
  "nextExactTask": "Apply the SAME adapter pattern (shared + prisma + composed, dynamic-import) to the remaining 3 runtime SQLite stores: signal-consent-transition-store, recommendation-context-store, funnel-event-store. Add their Prisma models (+migration via migrate dev), prove each against Postgres, rewire their API routes. Postgres: postgresql://aol:aol_local@localhost:55433 (docker aol-clean-room). Then: §15 idempotency-key vs business-duplicate split, §12/§18 status secret→HttpOnly session (drop ?ref=), §13/§19 distributed rate limit, §16/§20 serverless bundling guard test, §17 Prisma connection doc, §19-23 accessibility+visual convergence, §21 clean-room full proof, §25 final build.",
  "migrationTruth": {
    "p3018Reproduced": "yes, on empty Postgres 15.18 (aol_migcheck1, dropped)",
    "criticalFinding": "221 schema models vs ~90 migration-created tables = 131 untracked db-push baseline tables. 3 FK-exposed (DiagnosticJourney/Organisation/research_runs) + 1 ordering defect (RetainedDecision). A narrow 3-table bridge is WRONG; a full baseline of the pre-migration state is required.",
    "nextTaskExact": "Author baseline migration from the git-historical schema.prisma as of just-before-20260413 (NOT current schema): create the 131 baseline tables in HISTORICAL shape (DiagnosticJourney WITHOUT the 7 columns 20260423 adds), handle FK ordering (tables first, then satisfiable FKs), fix RetainedDecision ordering. Prove `prisma migrate deploy` exit 0 on empty DB (§6) + upgrade sim via `migrate resolve --applied` (§7). Postgres available at postgresql://aol:aol_local@localhost:55433.",
    "validator": "tests/database/migration-history.test.ts gates against NEW referenced-before-created tables."
  },
  "activeWriter": "CLAUDE (Codex inactive — usage exhausted; handed off at 878874a2f)",
  "completedSections": [
    "§0 ownership flip + exact-path staging protocol",
    "§1 full-suite-truth.json — at 956a983ff: 7444 passed / 1 failed (a concurrency TIMEOUT in verification-authority.test.ts, now fixed 30s→120s; earlier 8 estate failures now pass)",
    "§2 route-store-edges.json — 5 production routes → 4 sqlite stores; UNMAPPED_ROUTE_STORE_EDGE=0",
    "§3 removed ALLOW_SQLITE_RUNTIME production escape hatch — NODE_ENV=production always denies (test proves no backdoor)",
    "§4 migration-history-audit.json — P3018 root cause = db-push baseline gap; 3 untracked tables (DiagnosticJourney, Organisation, research_runs)",
    "§5 database-migration-reconciliation.md ADR — fresh-install baseline strategy + existing-db resolve strategy + validation commands",
    "PRIOR: §1 persistence-authority-matrix.json, §2 fail-closed doctrine, §10 funnel endpoint hardening, §34 deployment-preflight.json"
  ],
  "openSections": [
    "§6 AUTHOR + EXECUTE the baseline migration and prove BOTH paths (fresh-install migrate deploy exit 0; upgrade simulation) — needs clean-room Postgres",
    "§7-8 Pilot (+consent, recommendation-context, funnel) Prisma models + PrismaXStore adapters behind existing store interfaces + runtime route binding",
    "§9 idempotency key vs business duplicate classification (EXACT_RETRY/POSSIBLE_DUPLICATE/MATERIAL_RESUBMISSION/NEW_INTAKE) + canonicalisation + tests",
    "§10 optimistic concurrency on pilot transitions (version guard + race tests)",
    "§11 one typed Pilot API contract (Zod) shared server+client",
    "§12 secure status return (POST secret → HttpOnly session; remove ?ref=)",
    "§13 distributed (multi-instance) rate limit + funnel authenticity (server-issued journey id)",
    "§14 analytics≠truth firewall architecture test",
    "§15 static serverless filesystem guard (production import of local sqlite adapter → FAIL)",
    "§16 native better-sqlite3 bundling audit",
    "§17 serverless Postgres connection safety doc",
    "§18-21 assessment estate: accessibility (WCAG 2.2 AA) release gate, real responsive QA, visual convergence (/decision-centre, /tools/decision-delay-exposure)",
    "§22 currency via catalogue authority (remove hardcoded £)",
    "§23 operator pilot page IA (action above fold)",
    "§24 network loading/error/recovery states",
    "§25-26 notification continuity + privacy/retention",
    "§27 security preflight (CSP/cookies/CSRF via canonical middleware)",
    "§28 re-run full suite on final HEAD (expect 0 after timeout fix)",
    "§29 clean-room from tracked source (Postgres migrate deploy + tests + build)",
    "§30 real journey proof + restart persistence + concurrency conflict",
    "§31 GTM closure (verify artifacts, add buyer wedge; no fabricated data)",
    "§33 final-HEAD production build"
  ],
  "currentExactTask": "Migration-truth foundation complete (audit + strategy). Next is §6 execution.",
  "nextExactTask": "§6: bring up clean-room Postgres; generate baseline DDL via `prisma migrate diff --from-empty --to-schema-datamodel`; author 20260412000000_baseline_untracked_tables (CREATE TABLE IF NOT EXISTS for DiagnosticJourney/Organisation/research_runs + enums/indexes); prove `prisma migrate deploy` exits 0 on empty DB; then simulate upgrade path.",
  "schemaState": "prisma/schema.prisma unchanged this session (per directive: no new models until migration history reconciled). Provider=postgresql. 54 migrations; 3 untracked-baseline tables identified.",
  "migrationState": "AUDITED + STRATEGY DOCUMENTED; baseline migration NOT yet authored/executed (needs clean-room Postgres).",
  "cleanRoomState": "not run this session",
  "postgresState": "not exercised; Pilot not modelled/migrated",
  "buildState": "last green EXIT 0 / 783 pages at an EARLIER HEAD; NOT re-run at a5c13bf9b",
  "lastFullSuite": { "head": "956a983ff", "command": "NODE_ENV=test npx vitest run", "passed": 7444, "failed": 1, "exit": 1, "failureType": "timeout (fixed; needs re-run to confirm 0)" },
  "lastExitCodes": { "tsc": 0, "guardTest": 0, "verificationAuthorityIsolated": 0 },
  "prohibitedActions": ["git add -A", "git add .", "git reset --hard", "git clean -fd", "no push/deploy without owner approval", "no production DB mutation", "no fabricated production/revenue data", "no db push as final migration strategy", "no rewriting applied migration history"]
}

/* components/Intelligence/WeeklyDigestTemplate.tsx */
export const WeeklyDigestTemplate = ({ data }: { data: any }) => (
  <div className="p-16 bg-white text-black font-serif">
    <header className="border-b-4 border-black pb-8 mb-12 flex justify-between items-end">
      <div>
        <h1 className="text-3xl font-black uppercase tracking-tighter">The Abraham Report</h1>
        <p className="text-xs font-mono uppercase text-zinc-500">Weekly Strategic Intelligence Digest</p>
      </div>
      <div className="text-right text-[10px] font-mono font-bold">
        PERIOD: {data.weekRange}<br />
        REF: {data.generatedAt}
      </div>
    </header>

    <section className="mb-12">
      <h2 className="text-sm font-black bg-black text-white px-3 py-1 inline-block uppercase mb-6">01. Priority Engagement</h2>
      <div className="space-y-8">
        {data.dossiers.map((d: any, i: number) => (
          <div key={i} className="border-l-4 border-zinc-200 pl-6">
            <h3 className="text-xl font-bold uppercase tracking-tight mb-2">{d.title}</h3>
            <p className="text-sm text-zinc-700 italic mb-3">"{d.logic}"</p>
            <div className="flex gap-2">
               {d.criticalRisks.map((risk: string, j: number) => (
                 <span key={j} className="text-[9px] bg-zinc-100 px-2 py-1 uppercase font-bold text-zinc-500">Risk_{j+1}: {risk}</span>
               ))}
            </div>
          </div>
        ))}
      </div>
    </section>

    <footer className="mt-20 pt-8 border-t border-zinc-200 text-[9px] text-zinc-400 uppercase font-mono tracking-widest flex justify-between">
      <span>Authorized Principal Distribution Only</span>
      <span>Abraham of London // Directorate Oversight</span>
    </footer>
  </div>
);
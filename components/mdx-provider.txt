// components/MDXProvider.tsx (if you use @mdx-js/react)
import { MDXProvider as Base } from "@mdx-js/react";
import Image from "@/components/HighQImage";

const components = {
  img: (props: any) => <Image {...props} width={props.width ?? 1600} height={props.height ?? 900} />,
};

export default function MDXProvider({ children }: { children: React.ReactNode }) {
  return <Base components={components}>{children}</Base>;
}

---
---
// content/downloads/board-investor-one-pager-template.tsx
import * as React from "react";
import BrandFrame from "../../components/print/BrandFrame";
import { PullLine } from "../../components/PullLine"; // keep if you'll use it below
import EmbossedBrandMark from "../../components/EmbossedBrandMark";

// --- Metadata ---
export const metadata = {
  title: "Board Investor One-Pager Template",
  slug: "board-investor-one-pager",
  date: "2025-10-21",
  author: "Abraham of London",
  readTime: "3 min",
  category: "Business",
  type: "Download",
  subtitle: "Communicate clearly, concisely, and consistently with your board and investors.",
};

const BoardInvestorTemplate: React.FC = () => {
  return (
    <BrandFrame
      title={metadata.title}
      subtitle={metadata.subtitle}
      author={metadata.author}
      date={metadata.date}
      pageSize="A4"
    >
      {/* TODO: add your one-pager sections/content here.
          If you plan to use <PullLine />, drop it where needed. */}

      <div className="mt-12 text-center">
        <EmbossedBrandMark
          src="/assets/images/abraham-logo.jpg"
          alt="Abraham of London Brand Mark"
          width={80}
          height={80}
          className="inline-block"
        />
      </div>
    </BrandFrame>
  );
};

export default BoardInvestorTemplate;


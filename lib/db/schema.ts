// lib/db/schema/migrations.ts
export const MIGRATIONS_NOTE = "Migration SQL was moved to lib/db/migrations/001_create_short_interactions.sql to keep TypeScript valid.";

/**
 * Institutional Migration Runner (Optional/Future)
 * For now, run the SQL file directly in the Neon Console to establish the 
 * 'short_interactions' table for tracking engagement on your 163 briefs.
 */
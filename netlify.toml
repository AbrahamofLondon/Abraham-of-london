[build]
  command = "npm ci && npm run build"
  publish = ".next" # Specifies the directory Netlify should deploy from

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20.11.1"

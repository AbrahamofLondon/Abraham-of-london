[build]
  command = "pnpm run build:netlify"   # keep your existing build pipeline
  publish = "out"                      # or ".next" / "dist" depending on your setup

[build.environment]
  PNPM_FLAGS = "--no-frozen-lockfile"
  NODE_VERSION = "20.11.1"             # align with Netlify logs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // --- ADDED FIX: Dist Directory Setting ---
  // This allows Next.js to use a separate build directory when running dev/utility scripts,
  // which helps resolve EPERM (file lock) errors on Windows/CI.
  distDir: process.env.NEXT_DIST_DIR || '.next',
  // ------------------------------------------

  reactStrictMode: true,
  swcMinify: true,

  // Corrected images configuration
  images: {
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },

  trailingSlash: true,

  experimental: {
    // Keep esmExternals if you are using external modules that are ES modules
    esmExternals: true,
  },

  webpack: (config) => {
    config.module.rules.push({ test: /\.svg$/, use: ['@svgr/webpack'] });
    return config;
  },

  async headers() {
    return [
      {
        source: '/assets/images/:path*',
        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }],
      },
      {
        source: '/favicon/:path*',
        headers: [{ key: 'Cache-Control', value: 'public, max-age=31536000, immutable' }],
      },
    ];
  },
};

// Use the ES Module export syntax
export default nextConfig;
/** @type {import('next').NextConfig} */

// Optional wrapper: uses bundle analyzer only when available & enabled
const withBundleAnalyzer = (() => {
  try {
    // Enable with: ANALYZE=true npm run build
    return require('@next/bundle-analyzer')({
      enabled: process.env.ANALYZE === 'true',
    });
  } catch {
    // If the package isn't installed, just return identity
    return (cfg) => cfg;
  }
})();

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  // Safe: remove if your Next version complains
  experimental: {
    optimizePackageImports: ['framer-motion'],
  },
};

module.exports = withBundleAnalyzer(nextConfig);

{
  "name": "abraham-website",
  "version": "1.0.0",  // Keep your local version, it's newer and likely what you intend
  "private": true,    // Good to keep 'private: true' if it's not meant to be published
  "scripts": {
    "dev": "next dev --turbopack", // Keep your local 'dev' script with --turbopack
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    // Keep all dependencies from both sides, prioritize newer versions if conflicting
    "@tailwindcss/aspect-ratio": "^0.4.2",
    "@tailwindcss/forms": "^0.5.10",
    "@tailwindcss/typography": "^0.5.16",
    "next": "15.3.5", // Keep your local newer version
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    // Combine all devDependencies from both sides
    "@types/node": "^24.0.10",
    "framer-motion": "^12.23.0",
    "fs-extra": "^11.3.0",
    "@eslint/eslintrc": "^3",
    "autoprefixer": "^10.4.21",
    "critters": "^0.0.23",
    "eslint": "^9",
    "eslint-config-next": "15.3.4", // This eslint config is compatible with 15.3.x
    "postcss": "^8.5.6",
    "postcss-flexbugs-fixes": "^5.0.2",
    "postcss-preset-env": "^10.2.4"
  }
}
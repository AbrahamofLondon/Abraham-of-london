datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  directUrl  = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

// ============================================================================
// ENUMS
// ============================================================================

enum AlignmentBand {
  ALIGNED
  DRIFTING
  MISALIGNED
  DISORDERED
}

enum AlignmentDomain {
  IDENTITY
  DECISION
  ENVIRONMENT
  BEHAVIOUR
  EMOTIONAL_ORDER
  LEGACY
}

enum CorrectionStatus {
  MANDATED
  IN_PROGRESS
  LIQUIDATED
  ARCHIVED
}

enum MemberRole {
  ADMIN
  STRATEGIST
  MEMBER
  CLIENT
}

enum AccessTier {
  public
  member
  inner_circle
  restricted
  client
  legacy
  architect
  owner
  top_secret
}

enum MemberStatus {
  active
  inactive
  pending
  paused
  suspended
}

enum ContentType {
  Briefs
  Dossier
  Operational_Framework
  Landing
  Leadership
  Audit
  Research
  Sovereign_Intelligence
  Lexicon
  Intelligence
  Prints
  Strategy
}

enum LinkType {
  DEPENDENCY
  RELATED
  PREREQUISITE
}

enum AnnotationPriority {
  ROUTINE
  URGENT
  CRITICAL
}

enum InquiryStatus {
  PENDING
  REVIEWING
  CONTACTED
  CLOSED
}

enum StrategyIntakeStatus {
  PENDING
  IN_REVIEW
  ANALYZED
  ARCHIVED
}

enum DownloadEventType {
  PREVIEW
  DOWNLOAD
  PRINT
}

enum DownloadDeliveryMode {
  DIRECT
  EMAIL
  SECURE_LINK
}

enum DownloadContentType {
  PDF
  EPUB
  MARKDOWN
  ASSET
}

enum AccessType {
  VIEW
  DOWNLOAD
  METADATA
}

enum AuditSeverity {
  debug
  info
  warn
  error
  critical
}

enum SecurityEvent {
  LOGIN_SUCCESS
  LOGIN_FAILURE
  MFA_CHALLENGE
  PASSWORD_CHANGE
  UNAUTHORIZED_ACCESS
}

enum HttpMethod {
  GET
  POST
  PUT
  PATCH
  DELETE
}

enum SessionStatus {
  active
  expired
  revoked
}

enum KeyStatus {
  active
  revoked
  expired
}

enum MfaMethod {
  totp
  sms
  email
}

enum Permission {
  READ_BRIEFS
  DOWNLOAD_REPORTS
  ACCESS_VAULT
  MANAGE_ORGS
  ADMIN_ACCESS
}

enum DiagnosticSeverity {
  low
  moderate
  high
  critical
}

enum DiagnosticLifecycleStatus {
  draft
  completed
  archived
}

enum DiagnosticReportStatus {
  none
  pending
  paid
  generated
  failed
  revoked
}

enum DiagnosticArtifactKind {
  pdf
}

enum DiagnosticStorageProvider {
  local
  s3
}

enum DiagnosticRegenerationStatus {
  queued
  processing
  completed
  failed
  cancelled
}

enum GovernanceSeverity {
  CRITICAL
  HIGH
  WARNING
  INFO
  DEBUG
}

enum GovernanceStatus {
  ACTIVE
  PENDING
  RESOLVED
  SUPPRESSED
  ARCHIVED
}

enum UserRole {
  USER
  ADMIN
  OWNER
}

enum EntitlementType {
  TIER
  PRODUCT
  ARTIFACT
}

enum EntitlementStatus {
  ACTIVE
  REVOKED
  EXPIRED
}

enum AccessKeyStatus {
  ACTIVE
  REVOKED
  EXPIRED
  DEPLETED
}

enum InviteStatus {
  PENDING
  REDEEMED
  EXPIRED
  REVOKED
}

enum AuditActorType {
  USER
  SYSTEM
  ADMIN
}

// ============================================================================
// ORGANISATION & ENTERPRISE CAMPAIGN SYSTEM
// ============================================================================

model Organisation {
  id        String   @id @default(cuid())
  name      String
  slug      String   @unique
  sector    String?
  sizeBand  String?
  region    String?
  status    String   @default("active")
  metadata  String?  @default("{}")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  memberships   OrganisationMembership[]
  campaigns     AlignmentCampaign[]
  invites       OrganisationInvite[]
  interventions StrategicIntervention[]
  snapshots     AlignmentSnapshot[]
  retainerContracts RetainerContract[]

  @@index([name])
  @@index([status])
  @@index([slug])
}

model AlignmentCampaign {
  id                    String    @id @default(cuid())
  organisationId        String
  title                 String
  objective             String?
  status                String    @default("draft")
  stage                 String    @default("intake")
  diagnosticType        String    @default("enterprise")
  opensAt               DateTime?
  closesAt              DateTime?
  cadenceType           String    @default("ad_hoc")
  createdByMembershipId String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  metadata              String?   @default("{}")

  organisation        Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdByMembership OrganisationMembership? @relation("CampaignCreatedByMembership", fields: [createdByMembershipId], references: [id])

  correctionNodes        CorrectionNode[]
  participants           CampaignParticipant[]
  assessments            EnterpriseAssessment[]
  auditResponses         AuditResponse[]
  teamSnapshots          TeamAssessmentSnapshot[]
  organisationSnapshots  OrganisationAssessmentSnapshot[]
  constitutionalIntakes  ConstitutionalIntakeReport[]
  executiveRuns          ExecutiveReportingRun[]
  leadershipGapSnapshots LeadershipGapSnapshot[]
  reports                EnterpriseReport[]
  interventions          StrategicIntervention[]
  snapshot               AlignmentSnapshot?               @relation("CampaignAlignmentSnapshot")

  @@index([organisationId])
  @@index([status])
  @@index([diagnosticType])
  @@index([stage])
  @@index([opensAt])
  @@index([closesAt])
}

model AlignmentSnapshot {
  id             String   @id @default(cuid())
  campaignId     String   @unique
  organisationId String
  cohortSize     Int
  aggregatedData String
  finalizedAt    DateTime @default(now())

  campaign     AlignmentCampaign @relation("CampaignAlignmentSnapshot", fields: [campaignId], references: [id], onDelete: Cascade)
  organisation Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([organisationId])
  @@index([finalizedAt])
}

model OrganisationInvite {
  id             String       @id @default(cuid())
  organisationId String
  email          String
  tokenHash      String       @unique
  expiresAt      DateTime
  acceptedAt     DateTime?
  revokedAt      DateTime?
  metadata       String?      @default("{}")
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId])
  @@index([email])
  @@index([expiresAt])
  @@index([tokenHash])
  @@map("organisation_invites")
}

model OrganisationMembership {
  id             String   @id @default(cuid())
  organisationId String
  userId         String?
  email          String
  fullName       String?
  roleTitle      String?
  teamName       String?
  functionName   String?
  seniorityBand  String?
  isExecutive    Boolean  @default(false)
  role           String   @default("CONTRIBUTOR")
  status         String   @default("active")
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organisation Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  createdCampaigns AlignmentCampaign[]   @relation("CampaignCreatedByMembership")
  participants     CampaignParticipant[]

  @@unique([organisationId, email])
  @@index([organisationId])
  @@index([email])
  @@index([teamName])
  @@index([status])
}

model GovernanceLog {
  id          String   @id @default(cuid())
  action      String
  performedBy String
  target      String?
  details     String?
  createdAt   DateTime @default(now())
}

model User {
  id            String         @id @default(cuid())
  name          String?
  email         String?        @unique
  emailVerified DateTime?
  image         String?
  role          UserRole       @default(USER)

  accounts      Account[]

  entitlements  Entitlement[]
  accessKeyUses AccessKeyUse[]

  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  @@index([role])
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@map("user_accounts")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@map("verification_tokens")
}

model Entitlement {
  id          String             @id @default(cuid())
  userId      String
  user        User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  type        EntitlementType
  key         String
  status      EntitlementStatus  @default(ACTIVE)

  metadata    Json?

  issuedAt    DateTime           @default(now())
  startsAt    DateTime?
  expiresAt   DateTime?
  revokedAt   DateTime?
  issuedBy    String?
  revokedBy   String?
  reason      String?

  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([userId, type, status])
  @@index([key, type, status])
  @@unique([userId, type, key, status], map: "entitlement_active_uniqueness")
}

model AccessKey {
  id            String           @id @default(cuid())
  codeHash       String           @unique
  codePreview    String
  label          String?
  status         AccessKeyStatus  @default(ACTIVE)

  grants         Json
  metadata       Json?

  maxUses        Int              @default(1)
  uses           Int              @default(0)

  startsAt       DateTime?
  expiresAt      DateTime?
  revokedAt      DateTime?
  issuedBy       String?
  revokedBy      String?
  reason         String?

  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt

  usesLog        AccessKeyUse[]

  @@index([status])
  @@index([expiresAt])
}

model AccessKeyUse {
  id           String     @id @default(cuid())
  accessKeyId  String
  accessKey    AccessKey  @relation(fields: [accessKeyId], references: [id], onDelete: Cascade)

  userId       String
  user         User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  redeemedAt   DateTime   @default(now())
  ipAddress    String?
  userAgent    String?

  @@index([accessKeyId])
  @@index([userId])
  @@unique([accessKeyId, userId])
}

model AccessAuditLog {
  id            String         @id @default(cuid())
  actorType     AuditActorType
  actorUserId   String?
  actorEmail    String?

  action        String
  targetType    String
  targetKey     String?

  success       Boolean        @default(true)
  reason        String?
  metadata      Json?

  createdAt     DateTime       @default(now())

  @@index([action, createdAt])
  @@index([actorUserId, createdAt])
  @@index([targetType, targetKey])
}

model AccessInvite {
  id               String       @id @default(cuid())
  recipientEmail   String
  tokenHash        String       @unique
  grants           Json         // Array<{type, key}>
  status           InviteStatus @default(PENDING)

  issuedBy         String?
  issuedAt         DateTime     @default(now())
  expiresAt        DateTime?

  redeemedByUserId String?
  redeemedAt       DateTime?

  revokedAt        DateTime?
  revokedBy        String?
  reason           String?

  maxUses          Int          @default(1)
  uses             Int          @default(0)
  metadata         Json?

  emailSentAt      DateTime?
  emailError       String?

  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt

  @@index([recipientEmail])
  @@index([status])
  @@index([expiresAt])
  @@map("access_invites")
}

model CampaignParticipant {
  id              String    @id @default(cuid())
  campaignId      String
  membershipId    String?
  journeyId       String?
  respondentType  String?
  email           String
  inviteTokenHash String
  status          String    @default("invited")
  invitedAt       DateTime  @default(now())
  openedAt        DateTime?
  completedAt     DateTime?
  reminderCount   Int       @default(0)

  campaign   AlignmentCampaign       @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  membership OrganisationMembership? @relation(fields: [membershipId], references: [id])

  assessments EnterpriseAssessment[]

  @@unique([campaignId, email])
  @@index([campaignId])
  @@index([membershipId])
  @@index([journeyId])
  @@index([respondentType])
  @@index([email])
  @@index([status])
}

model AuditResponse {
  id         String   @id @default(cuid())
  campaignId String
  resonance  Int
  certainty  Int
  createdAt  DateTime @default(now())

  campaign AlignmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@map("audit_responses")
}

model EnterpriseAssessment {
  id                   String   @id @default(cuid())
  campaignId           String
  participantId        String
  organisationId       String
  teamName             String?
  isExecutive          Boolean  @default(false)
  answersJson          String
  totalScore           Int
  possibleScore        Int
  percentScore         Int
  band                 String
  weakestDomainsJson   String
  strongestDomainsJson String?
  domainScoresJson     String
  submittedAt          DateTime @default(now())

  campaign    AlignmentCampaign   @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  participant CampaignParticipant @relation(fields: [participantId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([organisationId])
  @@index([participantId])
  @@index([teamName])
  @@index([isExecutive])
  @@index([submittedAt])
}

model TeamAssessmentSnapshot {
  id                   String   @id @default(cuid())
  campaignId           String
  organisationId       String
  teamName             String
  respondentCount      Int
  totalScore           Int
  possibleScore        Int
  percentScore         Int
  band                 String
  weakestDomainsJson   String
  strongestDomainsJson String
  domainScoresJson     String
  varianceScoresJson   String
  generatedAt          DateTime @default(now())

  campaign AlignmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@unique([campaignId, teamName])
  @@index([campaignId])
  @@index([organisationId])
  @@index([teamName])
}

model TeamAssessmentCampaign {
  id                       String   @id @default(cuid())
  organisationId           String?
  sponsorUserId            String?
  slug                     String?  @unique
  title                    String
  mode                     String   @default("leader_estimate")
  status                   String   @default("draft")
  closesAt                 DateTime?
  minimumResponseThreshold Int      @default(3)
  anonymityMode            String   @default("anonymous")
  domainsJson              String
  leaderEstimateJson       String?
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt

  invites   TeamAssessmentInvite[]
  responses TeamAssessmentResponse[]
  aggregate TeamAssessmentAggregate?

  @@index([organisationId])
  @@index([sponsorUserId])
  @@index([mode])
  @@index([status])
  @@index([closesAt])
}

model TeamAssessmentInvite {
  id          String    @id @default(cuid())
  campaignId  String
  tokenHash   String    @unique
  email       String?
  roleLabel   String?
  status      String    @default("issued")
  issuedAt    DateTime  @default(now())
  openedAt    DateTime?
  submittedAt DateTime?
  expiresAt   DateTime?

  campaign TeamAssessmentCampaign  @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  response TeamAssessmentResponse?

  @@index([campaignId])
  @@index([email])
  @@index([status])
  @@index([expiresAt])
}

model TeamAssessmentResponse {
  id            String   @id @default(cuid())
  campaignId    String
  inviteId      String?  @unique
  respondentKey String
  answersJson   String
  submittedAt   DateTime @default(now())

  campaign TeamAssessmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)
  invite   TeamAssessmentInvite?  @relation(fields: [inviteId], references: [id], onDelete: SetNull)

  @@index([campaignId])
  @@index([respondentKey])
  @@index([submittedAt])
}

model TeamAssessmentAggregate {
  id             String   @id @default(cuid())
  campaignId     String   @unique
  respondentCount Int
  invitedCount    Int
  completionRate  Float
  confidence      Float
  claimLevel      String
  domainsJson     String
  generatedAt     DateTime @default(now())
  updatedAt       DateTime @updatedAt

  campaign TeamAssessmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([claimLevel])
  @@index([generatedAt])
}

model OrganisationAssessmentSnapshot {
  id                   String   @id @default(cuid())
  campaignId           String   @unique
  organisationId       String
  respondentCount      Int
  invitedCount         Int
  completionRate       Int
  totalScore           Int
  possibleScore        Int
  percentScore         Int
  band                 String
  weakestDomainsJson   String
  strongestDomainsJson String
  domainScoresJson     String
  varianceScoresJson   String
  fragilitySignal      String?
  generatedAt          DateTime @default(now())

  campaign AlignmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([organisationId])
}

model LeadershipGapSnapshot {
  id                      String   @id @default(cuid())
  campaignId              String   @unique
  organisationId          String
  overallGapPercent       Int
  domainGapsJson          String
  interpretationFlagsJson String
  generatedAt             DateTime @default(now())

  campaign AlignmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([organisationId])
}

model EnterpriseReport {
  id             String   @id @default(cuid())
  campaignId     String
  organisationId String
  reportType     String
  filename       String
  storagePath    String?
  reportVersion  String
  generatedAt    DateTime @default(now())

  campaign AlignmentCampaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@index([campaignId])
  @@index([organisationId])
  @@index([reportType])
}

// ============================================================================
// DIAGNOSTIC ARTEFACT REGISTRY / LINEAGE / RETENTION
// ============================================================================

model DiagnosticRecord {
  id             String             @id @default(cuid())
  diagnosticType String
  title          String?
  score          Int
  severity       DiagnosticSeverity
  verdict        String
  responsesJson  String
  notes          String?

  userEmail String?
  userId    String?

  status       DiagnosticLifecycleStatus @default(completed)
  reportStatus DiagnosticReportStatus    @default(none)
  reportTier   String?

  latestVersion String?
  generatedAt   DateTime?
  failedAt      DateTime?
  failureReason String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  reportOrders  DiagnosticReportOrder[]
  artifacts     DiagnosticArtifact[]
  accessGrants  DiagnosticArtifactAccessGrant[]
  lineageEvents DiagnosticLineageEvent[]
  auditEvents   DiagnosticAuditEvent[]
  regenJobs     DiagnosticRegenerationJob[]

  @@index([userEmail, createdAt])
  @@index([userId, createdAt])
  @@index([diagnosticType, createdAt])
  @@index([reportStatus, createdAt])
  @@index([status, createdAt])
  @@map("diagnostic_records")
}

model DiagnosticReportOrder {
  id                 String @id @default(cuid())
  diagnosticRecordId String

  stripeSessionId String? @unique
  stripePaymentId String?

  reportTier String
  amount     Int
  currency   String @default("gbp")
  status     String @default("pending")

  userEmail String?
  userId    String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  diagnosticRecord DiagnosticRecord @relation(fields: [diagnosticRecordId], references: [id], onDelete: Cascade)

  @@index([diagnosticRecordId])
  @@index([status, createdAt])
  @@map("diagnostic_report_orders")
}

model DiagnosticArtifact {
  id            String                 @id @default(cuid())
  diagnosticRef String
  diagnosticId  String
  reportId      String?
  version       String
  kind          DiagnosticArtifactKind @default(pdf)

  fileName   String
  mimeType   String
  byteLength Int
  sha256     String

  storageProvider DiagnosticStorageProvider
  objectKey       String
  bucket          String?

  etag       String?
  publicPath String?

  isRevoked     Boolean   @default(false)
  revokedAt     DateTime?
  revokedReason String?

  retentionClass String    @default("standard")
  expiresAt      DateTime?

  createdAt DateTime @default(now())
  createdBy String?

  diagnostic DiagnosticRecord @relation(fields: [diagnosticId], references: [id], onDelete: Cascade)

  @@unique([diagnosticRef, version, kind])
  @@index([diagnosticRef])
  @@index([diagnosticId])
  @@index([version])
  @@index([isRevoked])
  @@map("diagnostic_artifacts")
}

model DiagnosticRegenerationJob {
  id            String  @id @default(cuid())
  diagnosticRef String
  diagnosticId  String?
  version       String?

  status  DiagnosticRegenerationStatus @default(queued)
  attempt Int                          @default(0)
  error   String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  diagnostic DiagnosticRecord? @relation(fields: [diagnosticId], references: [id], onDelete: SetNull)

  @@index([diagnosticRef])
  @@index([diagnosticId])
  @@index([status, createdAt])
  @@map("diagnostic_regeneration_jobs")
}

model DiagnosticAuditEvent {
  id            String   @id @default(cuid())
  diagnosticRef String?
  diagnosticId  String?
  action        String
  actor         String?
  metadata      String?
  createdAt     DateTime @default(now())

  diagnostic DiagnosticRecord? @relation(fields: [diagnosticId], references: [id], onDelete: SetNull)

  @@index([diagnosticRef])
  @@index([diagnosticId])
  @@index([createdAt])
  @@map("diagnostic_audit_events")
}

model DiagnosticArtifactAccessGrant {
  id             String    @id @default(cuid())
  diagnosticRef  String
  diagnosticId   String
  artifactId     String
  granteeEmail   String
  entitlementKey String    @unique
  status         String    @default("active")
  expiresAt      DateTime?
  createdAt      DateTime  @default(now())
  createdBy      String?

  diagnostic DiagnosticRecord @relation(fields: [diagnosticId], references: [id], onDelete: Cascade)

  @@index([diagnosticRef])
  @@index([diagnosticId])
  @@index([granteeEmail])
  @@index([diagnosticRef, granteeEmail])
  @@map("diagnostic_artifact_access_grants")
}

model DiagnosticLineageEvent {
  id               String   @id @default(cuid())
  diagnosticRef    String
  diagnosticId     String
  artifactId       String?
  parentArtifactId String?
  eventType        String
  version          String?
  actor            String?
  metadata         String?
  createdAt        DateTime @default(now())

  diagnostic DiagnosticRecord @relation(fields: [diagnosticId], references: [id], onDelete: Cascade)

  @@index([diagnosticRef])
  @@index([diagnosticId])
  @@index([createdAt])
  @@map("diagnostic_lineage_events")
}

model ProofEvidence {
  id                String   @id @default(cuid())
  sourceStage       String
  proofType         String
  routeResultType   String?
  accuracyScore     String?
  usefulnessScore   String?
  nextStepChanged   Boolean?
  actionIntent      String?
  outcomeCategory   String?
  mostAccuratePart  String?
  paidSpecificity   String?
  consequenceClear  Boolean?
  justifiedAction   Boolean?
  decisionClarity   String?
  nextMoveClear     Boolean?
  freeTextRaw       String?
  anonymisedSummary String?
  displayLabel      String?
  userType          String?
  organisationType  String?
  sourceOrigin      String?
  isPaidStage       Boolean  @default(false)
  followupAt        DateTime?
  approvalStatus    String   @default("PENDING")
  displayStatus     String   @default("HIDDEN")
  sourceKind        String   @default("SELF_REPORTED")
  adminNotes        String?
  metadataJson      String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([sourceStage, createdAt])
  @@index([proofType, createdAt])
  @@index([approvalStatus, displayStatus])
  @@index([followupAt])
  @@map("proof_evidence")
}

model ClientEntitlement {
  id          String    @id @default(cuid())
  email       String
  productCode String
  tier        String
  status      String    @default("active")
  source      String    @default("manual")
  externalRef String?
  startsAt    DateTime  @default(now())
  endsAt      DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([email])
  @@index([productCode])
  @@index([email, productCode, status])
}

model BillingCustomer {
  id               String   @id @default(cuid())
  email            String   @unique
  name             String?
  stripeCustomerId String?  @unique
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

model OperationalIncident {
  id          String    @id @default(cuid())
  title       String
  severity    String
  status      String    @default("open")
  category    String?
  summary     String?
  details     String?
  source      String?
  firstSeenAt DateTime  @default(now())
  lastSeenAt  DateTime  @default(now())
  resolvedAt  DateTime?
  owner       String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([status, severity])
  @@index([firstSeenAt])
}

model ArtifactManifest {
  id             String    @id @default(cuid())
  artifactId     String
  subjectEmail   String
  artifactType   String
  version        String?
  storageDriver  String
  storagePath    String
  checksumSha256 String?
  mimeType       String?
  byteSize       Int?
  isRevoked      Boolean   @default(false)
  revokedAt      DateTime?
  retentionClass String?
  expiresAt      DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt

  @@index([subjectEmail, createdAt])
  @@index([artifactId])
  @@index([isRevoked, expiresAt])
}

model JobDeadLetter {
  id           String    @id @default(cuid())
  queue        String
  jobType      String
  reason       String
  payloadJson  String?
  fingerprint  String?
  source       String?
  actor        String?
  severity     String    @default("medium")
  retryable    Boolean   @default(true)
  status       String    @default("open")
  replayNote   String?
  metadataJson String?
  replayedAt   DateTime?
  resolvedAt   DateTime?
  createdAt    DateTime  @default(now())

  @@index([queue, status, createdAt])
  @@index([jobType, status, createdAt])
}

model ServiceLevelSnapshot {
  id              String   @id @default(cuid())
  serviceName     String
  windowStart     DateTime
  windowEnd       DateTime
  availabilityPct Float
  errorRatePct    Float?
  p95Ms           Float?
  requestCount    Int      @default(0)
  breachCount     Int      @default(0)
  metadata        String?
  createdAt       DateTime @default(now())

  @@index([serviceName, windowStart, windowEnd])
}

model RunbookEntry {
  id           String   @id @default(cuid())
  slug         String   @unique
  title        String
  category     String
  summary      String
  severity     String?
  bodyMarkdown String
  isActive     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

// ============================================================================
// DECISION RECOMMENDATION + GOVERNANCE SIGNAL SYSTEM
// ============================================================================

model DecisionRecommendationSession {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  sessionKey   String  @unique
  submissionId String?
  anonymousId  String?
  email        String?
  company      String?

  route          String?
  priority       String?
  temperature    String?
  orgState       String?
  readinessTier  String?
  authorityType  String?
  revenueBand    String?
  sector         String?
  marketRiskBand String?

  fusedScore      Float?
  routeConfidence Float?

  recommendationCount Int       @default(0)
  converted           Boolean   @default(false)
  conversionType      String?
  conversionAt        DateTime?

  metadata Json?

  impressions DecisionRecommendationImpression[]
  clicks      DecisionRecommendationClick[]
  conversions DecisionRecommendationConversion[]
  followups   DecisionSessionFollowup[]

  @@index([sessionKey])
  @@index([submissionId])
  @@index([anonymousId])
  @@index([email])
  @@index([route])
  @@index([readinessTier])
  @@index([authorityType])
  @@index([converted])
  @@index([createdAt])
  @@map("decision_recommendation_sessions")
}

model DecisionRecommendationImpression {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  sessionId String
  session   DecisionRecommendationSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  assetId    String
  assetTitle String
  assetHref  String?
  assetKind  String

  rank               Int
  matchScore         Float
  metadataConfidence Float?

  reasons         Json?
  contextSnapshot Json?

  clicks DecisionRecommendationClick[]

  @@index([sessionId])
  @@index([assetId])
  @@index([assetKind])
  @@index([createdAt])
  @@map("decision_recommendation_impressions")
}

model DecisionRecommendationClick {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  sessionId String
  session   DecisionRecommendationSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  impressionId String?
  impression   DecisionRecommendationImpression? @relation(fields: [impressionId], references: [id], onDelete: SetNull)

  assetId    String
  assetTitle String
  assetHref  String?
  assetKind  String

  rank       Int
  matchScore Float

  isConverted    Boolean @default(false)
  conversionType String?

  conversion DecisionRecommendationConversion?

  @@index([sessionId])
  @@index([impressionId])
  @@index([assetId])
  @@index([assetKind])
  @@index([isConverted])
  @@index([createdAt])
  @@map("decision_recommendation_clicks")
}

model DecisionRecommendationConversion {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  sessionId String
  session   DecisionRecommendationSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  clickId String?                      @unique
  click   DecisionRecommendationClick? @relation(fields: [clickId], references: [id], onDelete: SetNull)

  conversionType  String
  conversionValue Float?

  assetId    String?
  assetTitle String?
  assetHref  String?
  assetKind  String?

  metadata Json?

  @@index([sessionId])
  @@index([clickId])
  @@index([assetId])
  @@index([assetKind])
  @@index([conversionType])
  @@index([createdAt])
  @@map("decision_recommendation_conversions")
}

model DecisionSessionFollowup {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  sessionId String
  session   DecisionRecommendationSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  routeBefore         String?
  routeAfter          String?
  readinessTierBefore String?
  readinessTierAfter  String?
  authorityTypeBefore String?
  authorityTypeAfter  String?

  clarityDelta           Float?
  authorityDelta         Float?
  readinessDelta         Float?
  routeImproved          Boolean @default(false)
  convertedAfterGuidance Boolean @default(false)

  metadata Json?

  @@index([sessionId])
  @@index([createdAt])
  @@map("decision_session_followups")
}

model DecisionAssetEfficacy {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assetId    String  @unique
  assetTitle String
  assetHref  String?
  assetKind  String

  impressions         Int @default(0)
  clicks              Int @default(0)
  conversions         Int @default(0)
  assistedConversions Int @default(0)

  routeImprovements     Int   @default(0)
  readinessImprovements Int   @default(0)
  clarityImprovements   Float @default(0)
  authorityImprovements Float @default(0)

  efficacyScore           Float @default(0)
  decisionUsefulnessScore Float @default(0)
  confidenceScore         Float @default(0)

  lastEvaluatedAt DateTime?
  metadata        Json?

  contextPerformances DecisionAssetContextPerformance[]
  signalRegistry      DecisionSignalRegistry[]
  governanceAlerts    DecisionGovernanceAlert[]

  @@index([assetKind])
  @@index([efficacyScore])
  @@index([decisionUsefulnessScore])
  @@index([confidenceScore])
  @@index([lastEvaluatedAt])
  @@map("decision_asset_efficacy")
}

model DecisionAssetContextPerformance {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assetId    String
  assetTitle String
  assetHref  String?
  assetKind  String

  contextType  String
  contextValue String

  impressions         Int @default(0)
  clicks              Int @default(0)
  conversions         Int @default(0)
  assistedConversions Int @default(0)

  routeImprovements     Int   @default(0)
  readinessImprovements Int   @default(0)
  clarityGain           Float @default(0)
  authorityGain         Float @default(0)

  contextualWeight Float @default(1)
  confidenceScore  Float @default(0)
  usefulnessScore  Float @default(0)

  lastEvaluatedAt DateTime?
  metadata        Json?

  signalRegistry          DecisionSignalRegistry[]
  governanceAlerts        DecisionGovernanceAlert[]
  DecisionAssetEfficacy   DecisionAssetEfficacy?    @relation(fields: [decisionAssetEfficacyId], references: [id])
  decisionAssetEfficacyId String?

  @@unique([assetId, contextType, contextValue])
  @@index([contextType, contextValue])
  @@index([assetId])
  @@index([assetKind])
  @@index([usefulnessScore])
  @@index([confidenceScore])
  @@index([contextualWeight])
  @@index([lastEvaluatedAt])
  @@map("decision_asset_context_performance")
}

// ============================================================================
// NEW: GOVERNANCE DEFINITION ENGINE
// ============================================================================

model GovernanceMetricDefinition {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  key         String   @unique // e.g., "RESONANCE_THRESHOLD" or "CERTAINTY_MIN"
  name        String
  description String?
  category    String   // e.g., "Audit", "Strategic", "Operational"
  
  dataType    String   @default("PERCENTAGE") 
  thresholdHi Float?   // Upper bound for "Healthy"
  thresholdLo Float?   // Lower bound for "Critical"

  active      Boolean  @default(true)
  
  // Link to signals to see which entities are currently being measured by this metric
  signals     DecisionSignalRegistry[]

  @@index([category])
  @@map("governance_metric_definitions")
}

// ============================================================================
// EXPANDED: SIGNAL REGISTRY (Synchronized with your Decision models)
// ============================================================================

model DecisionSignalRegistry {
  id              String   @id @default(cuid())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  registryKey     String   @unique // Unique identifier for the specific observation trace

  // POLYMORPHIC LINKING
  // This allows the registry to track Campaign Assets, Participants, or even API routes
  assetId         String
  assetTitle      String
  assetHref       String?
  assetKind       String

  contextType     String   // e.g., "PARTICIPANT_AUDIT"
  contextValue    String   // e.g., the participantId

  status          String   @default("ACTIVE")
  highestSeverity String   @default("info") // info, warning, critical

  healthScore     Float    @default(0)
  driftScore      Float    @default(0)
  resonanceScore  Float    @default(0)
  confidenceScore Float?
  resonanceBand   String?

  alertCount      Int      @default(0)
  lastEvaluatedAt DateTime?

  // RELATION TO METRIC DEFINITION
  metricKey       String?
  metric          GovernanceMetricDefinition? @relation(fields: [metricKey], references: [key], onDelete: SetNull)

  // RELATION TO EFFICACY (Existing)
  assetEfficacyId String?
  assetEfficacy   DecisionAssetEfficacy? @relation(fields: [assetEfficacyId], references: [id], onDelete: SetNull)

  // RELATION TO CONTEXT PERFORMANCE (Existing)
  contextPerformanceId String?
  contextPerformance   DecisionAssetContextPerformance? @relation(fields: [contextPerformanceId], references: [id], onDelete: SetNull)

  metrics         Json?
  alerts          Json?
  metadata        Json?

  governanceAlerts DecisionGovernanceAlert[]

  @@unique([assetId, contextType, contextValue])
  @@index([assetId])
  @@index([status])
  @@index([highestSeverity])
  @@map("decision_signal_registry")
}

model DecisionGovernanceAlert {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assetId    String
  assetTitle String
  assetHref  String?
  assetKind  String

  contextType  String?
  contextValue String?

  registryKey String?

  alertType String
  metricKey String?

  severity String
  status   String @default("ACTIVE")

  message String

  previousValue Float?
  currentValue  Float?
  deltaValue    Float?
  deltaPercent  Float?

  firstDetectedAt DateTime  @default(now())
  lastDetectedAt  DateTime  @default(now())
  resolvedAt      DateTime?

  occurrenceCount Int     @default(1)
  isActive        Boolean @default(true)

  metadata Json?

  assetEfficacyId String?
  assetEfficacy   DecisionAssetEfficacy? @relation(fields: [assetEfficacyId], references: [id], onDelete: SetNull)

  contextPerformanceId String?
  contextPerformance   DecisionAssetContextPerformance? @relation(fields: [contextPerformanceId], references: [id], onDelete: SetNull)

  signalRegistryId String?
  signalRegistry   DecisionSignalRegistry? @relation(fields: [signalRegistryId], references: [id], onDelete: SetNull)

  @@index([assetId])
  @@index([assetKind])
  @@index([contextType, contextValue])
  @@index([registryKey])
  @@index([alertType])
  @@index([metricKey])
  @@index([severity])
  @@index([status])
  @@index([isActive])
  @@index([createdAt])
  @@index([lastDetectedAt])
  @@map("decision_governance_alerts")
}

// ============================================================================
// CORE INDIVIDUAL ASSESSMENT MODELS
// ============================================================================

model PurposeAlignmentAssessment {
  id                 String        @id @default(cuid())
  userId             String?
  sessionKey         String?
  title              String        @default("Purpose Alignment Assessment")
  notes              String?
  totalScore         Int
  possibleScore      Int
  percentScore       Int
  band               AlignmentBand
  fragilitySignal    String?       @default("LOW")
  dissonanceArea     Int?          @default(0)
  varianceScores     String?
  weakestDomains     String
  strengths          String
  corrections        String
  answers            String
  domainScores       String
  canonicalResult    String?
  responseMode       String        @default("dual_axis")
  reportVersion      String        @default("1.0.0")
  sourceInstrumentId String        @default("IA-PAC-001")
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt

  reports      PurposeAlignmentReport[]
  reminderLogs PurposeAlignmentReminderLog[]

  @@index([userId, createdAt])
  @@index([sessionKey, createdAt])
}

model PurposeAlignmentReport {
  id            String   @id @default(cuid())
  assessmentId  String
  filename      String
  storagePath   String?
  generatedAt   DateTime @default(now())
  reportVersion String   @default("1.0.0")

  assessment PurposeAlignmentAssessment @relation(fields: [assessmentId], references: [id], onDelete: Cascade)

  @@index([assessmentId, generatedAt])
}

model PurposeAlignmentReminderPreference {
  id          String    @id @default(cuid())
  userId      String?
  sessionKey  String?
  email       String?
  isEnabled   Boolean   @default(true)
  cadenceDays Int       @default(30)
  lastSentAt  DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  user InnerCircleMember?            @relation(fields: [userId], references: [id], onDelete: SetNull)
  logs PurposeAlignmentReminderLog[]

  @@unique([userId, sessionKey])
  @@index([userId])
  @@index([sessionKey])
  @@index([isEnabled])
  @@map("purpose_alignment_reminder_preferences")
}

model PurposeAlignmentReminderLog {
  id           String    @id @default(cuid())
  userId       String?
  sessionKey   String?
  email        String?
  assessmentId String
  status       String    @default("queued")
  channel      String    @default("in_app")
  scheduledFor DateTime
  sentAt       DateTime?
  payload      String
  error        String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  preferenceId String?

  assessment PurposeAlignmentAssessment          @relation(fields: [assessmentId], references: [id], onDelete: Cascade)
  user       InnerCircleMember?                  @relation(fields: [userId], references: [id], onDelete: SetNull)
  preference PurposeAlignmentReminderPreference? @relation(fields: [preferenceId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([sessionKey])
  @@index([status])
  @@index([scheduledFor])
  @@index([assessmentId])
  @@index([preferenceId])
  @@map("purpose_alignment_reminder_logs")
}

model DealFlowSubmission {
  id                         String   @id @default(cuid())
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  name                       String?
  email                      String?
  revenue                    String
  problem                    String
  urgency                    String
  authority                  String
  score                      Int
  route                      String
  aiScore                    Float?
  aiConfidence               Float?
  aiSummary                  String?
  aiIntent                   String?
  aiDealQuality              String?
  sessionDepth               Int?
  timeOnSite                 Int?
  returnVisitor              Boolean?
  predictedWinProbability    Float?
  predictedCloseVelocityDays Int?
  predictedExpectedRevenue   Float?
  predictedPriority          String?
  predictedTemperature       String?
  predictedNextAction        String?
  predictiveRationale        String?
  ipAddress                  String?
  userAgent                  String?
  referrer                   String?
  status                     String   @default("NEW")
  priority                   String?
  userId                     String?
}

model DecisionAssetPerformance {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assetId    String  @unique
  assetTitle String
  assetHref  String?
  assetKind  String

  impressions Int @default(0)
  clicks      Int @default(0)
  conversions Int @default(0)

  clickThroughRate Float @default(0)
  conversionRate   Float @default(0)
  adaptiveWeight   Float @default(1)

  lastInteractionAt DateTime?
  metadata          String?
}

// ============================================================================
// IDENTITY & ACCESS MODELS
// ============================================================================

model InnerCircleMember {
  id              String       @id @default(cuid()) @map("id")
  email           String?      @unique @map("email")
  name            String?      @map("name")
  role            MemberRole   @default(MEMBER) @map("role")
  status          MemberStatus @default(active) @map("status")
  tier            AccessTier   @default(member) @map("tier")
  flags           String?      @map("flags")
  metadata        String?      @default("{}") @map("metadata")
  emailHash       String       @unique @map("email_hash")
  emailHashPrefix String?      @map("email_hash_prefix")
  passwordHash    String?      @map("password_hash")
  lastIp          String?      @map("last_ip")
  viewCount       Int          @default(0) @map("view_count")
  createdAt       DateTime     @default(now()) @map("created_at")
  updatedAt       DateTime     @updatedAt @map("updated_at")
  lastSeenAt      DateTime     @default(now()) @map("last_seen_at")

  permissions String? @default("[]") @map("permissions")

  adminSessions   AdminSession[]
  apiKeys         ApiKey[]
  apiLogs         ApiLog[]
  downloadEvents  DownloadAuditEvent[]   @relation("MemberDownloadEvents")
  frameworkAccess FrameworkAccessLog[]
  keys            InnerCircleKey[]
  mfaSetup        MfaSetup?
  pageViews       PageView[]
  premiumTokens   PremiumDownloadToken[]
  annotations     PrivateAnnotation[]
  rateLimitLogs   RateLimitLog[]
  securityLogs    SecurityLog[]
  sessions        Session[]
  inquiries       StrategyInquiry[]
  strategyIntakes StrategyIntake[]
  preferences     UserPreference?

  alignmentReminderPreferences PurposeAlignmentReminderPreference[]
  alignmentReminderLogs        PurposeAlignmentReminderLog[]

  @@index([status])
  @@index([tier])
  @@index([role])
  @@index([emailHash])
  @@index([lastSeenAt])
  @@map("inner_circle_members")
}

model AdminSession {
  id        String            @id @default(cuid())
  memberId  String
  token     String            @unique
  createdAt DateTime          @default(now())
  expiresAt DateTime
  member    InnerCircleMember @relation(fields: [memberId], references: [id], onDelete: Cascade)

  @@index([memberId])
  @@map("admin_sessions")
}

model ApiKey {
  id        String            @id @default(cuid())
  name      String
  keyHash   String            @unique
  memberId  String
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt
  member    InnerCircleMember @relation(fields: [memberId], references: [id], onDelete: Cascade)

  @@index([memberId])
  @@map("api_keys")
}

model ApiLog {
  id             String             @id @default(cuid())
  endpoint       String
  method         HttpMethod
  statusCode     Int
  responseTimeMs Int                @default(0)
  ipAddress      String?
  userAgent      String?
  actorId        String?
  actorEmail     String?
  metadata       String?            @default("{}")
  createdAt      DateTime           @default(now())
  memberId       String?
  member         InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([createdAt])
  @@map("api_logs")
}

model InnerCircleKey {
  id            String            @id @default(cuid())
  keyHash       String            @unique
  status        KeyStatus         @default(active)
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
  expiresAt     DateTime
  keySuffix     String?
  keyType       String
  lastUsedAt    DateTime?
  metadata      String?           @default("{}")
  revokedAt     DateTime?
  revokedReason String?
  memberId      String
  member        InnerCircleMember @relation(fields: [memberId], references: [id], onDelete: Cascade)

  @@index([memberId])
  @@map("inner_circle_keys")
}

model MfaSetup {
  id            String            @id @default(cuid())
  enabled       Boolean           @default(false)
  methods       String?           @default("[\"totp\"]")
  totpSecret    String?
  totpVerified  Boolean           @default(false)
  backupCodes   String?           @default("[]")
  phoneNumber   String?
  phoneVerified Boolean           @default(false)
  emailVerified Boolean           @default(false)
  recoveryEmail String?
  lastUsedAt    DateTime?
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt
  userId        String            @unique
  member        InnerCircleMember @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("mfa_setups")
}

model Session {
  id        String             @id @default(cuid())
  sessionId String             @unique
  status    SessionStatus      @default(active)
  expiresAt DateTime
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt
  memberId  String?
  ipAddress String?            @map("ip_address")
  metadata  String?            @default("{}")
  userAgent String?            @map("user_agent")
  member    InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([memberId])
  @@map("sessions")
}

model SecurityLog {
  id        String             @id @default(cuid())
  event     SecurityEvent
  severity  AuditSeverity      @default(info)
  action    String
  details   String?            @default("{}")
  ipAddress String?
  userAgent String?
  createdAt DateTime           @default(now())
  memberId  String?
  member    InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([createdAt])
  @@map("security_logs")
}

model PageView {
  id        String             @id @default(cuid())
  path      String
  memberId  String?
  createdAt DateTime           @default(now())
  member    InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([path])
  @@index([memberId])
  @@map("page_views")
}

model RateLimitLog {
  id        String             @id @default(cuid())
  key       String
  memberId  String?
  endpoint  String
  method    String?
  allowed   Boolean
  remaining Int
  limit     Int
  resetAt   DateTime
  ipAddress String?
  userAgent String?
  createdAt DateTime           @default(now())
  bucket    String?
  requestId String?
  sessionId String?
  member    InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([key])
  @@map("rate_limit_logs")
}

model SystemAuditLog {
  id           String        @id @default(cuid())
  action       String
  severity     AuditSeverity @default(info)
  actorId      String?
  resourceId   String?
  ipAddress    String?
  userAgent    String?
  metadata     String?       @default("{}")
  createdAt    DateTime      @default(now())
  actorEmail   String?
  actorType    String?       @default("system") @map("actor_type")
  category     String?       @map("category")
  durationMs   Int?          @map("duration_ms")
  errorMessage String?       @map("error_message")
  requestId    String?       @map("request_id")
  resourceName String?       @map("resource_name")
  resourceType String?       @map("resource_type")
  sessionId    String?       @map("session_id")
  status       String?       @default("success") @map("status")
  subCategory  String?       @map("sub_category")
  tags         String?       @default("[]") @map("tags")

  @@index([action])
  @@map("system_audit_logs")
}

model UserPreference {
  id        String            @id @default(cuid())
  memberId  String            @unique
  settings  String            @default("{}")
  createdAt DateTime          @default(now())
  updatedAt DateTime          @updatedAt
  member    InnerCircleMember @relation(fields: [memberId], references: [id], onDelete: Cascade)

  @@map("user_preferences")
}

model UserIntegration {
  id           String    @id @default(cuid())
  userId       String
  provider     String    // "google" | "slack" | "jira" | "linear" | "github" | "notion"
  accessToken  String    // encrypted at rest via lib/integrations/encryption.ts
  refreshToken String?   // encrypted at rest
  tokenExpiry  DateTime
  scopes       String    @default("")
  status       String    @default("active") // "active" | "expired" | "revoked"
  lastSyncAt   DateTime?
  metadata     String?   @default("{}")
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@unique([userId, provider])
  @@index([userId])
  @@index([provider])
  @@index([status])
  @@map("user_integrations")
}

model BehavioralSignalSnapshot {
  id                     String   @id @default(cuid())
  userId                 String
  organisationId         String?
  accountId              String?
  source                 String
  sourceLabel            String?
  evidencePosture        String?
  signalKey              String
  signalValueJson        Json
  confidence             Float?
  evidenceWindowStart    DateTime?
  evidenceWindowEnd      DateTime?
  generatedAt            DateTime @default(now())
  integrationConnectedAt DateTime?
  rawCountBasisJson      Json?
  metadataJson           Json?
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  @@index([userId, generatedAt])
  @@index([userId, source, signalKey])
  @@index([userId, source, signalKey, generatedAt])
  @@index([organisationId, generatedAt])
  @@index([accountId, generatedAt])
  @@map("behavioral_signal_snapshots")
}

model StrategyInquiry {
  id        String             @id @default(cuid())
  name      String
  email     String
  intent    String
  status    InquiryStatus      @default(PENDING)
  metadata  String?            @default("{}")
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt
  memberId  String?
  member    InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([email])
  @@map("strategy_inquiries")
}

model StrategyIntake {
  id              String               @id @default(cuid())
  fullName        String
  organisation    String
  dependencyLevel String
  volatility      String
  readinessScore  Int                  @default(5)
  payload         String
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  emailHash       String?
  memberId        String?
  analysisNotes   String?              @default("{}")
  analysisVersion String?
  analyzedAt      DateTime?
  analyzedBy      String?
  score           Int?
  status          StrategyIntakeStatus @default(PENDING)
  member          InnerCircleMember?   @relation(fields: [memberId], references: [id])

  @@index([organisation])
  @@map("strategy_intakes")
}

model ConstitutionalIntakeReport {
  id                      String   @id @default(cuid())
  source                  String
  sessionKey              String?
  respondentKey           String?
  campaignId              String?
  email                   String?
  organisation            String?
  ipAddress               String?
  userAgent               String?

  // ✅ Fixed: Removed @db.LongText for SQLite compatibility
  answersJson             String
  reportJson              String
  constitutionalInputJson String
  decisionJson            String
  routeSummaryJson        String
  bridgeJson              String?

  route                   String?
  confidence              Float?
  posture                 String?
  readinessTier           String?
  authorityType           String?
  seriousnessScore        Int?
  completionPercent       Int?

  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  campaign                AlignmentCampaign? @relation(fields: [campaignId], references: [id])

  @@index([createdAt])
  @@index([campaignId])
  @@index([sessionKey])
  @@index([respondentKey])
  @@index([email])
  @@index([organisation])
  @@index([route])
}

model ExecutiveReportingRun {
  id                 String   @id @default(cuid())
  runKey             String   @unique
  campaignId         String?
  email              String
  fullName           String?
  organisation       String?
  role               String?
  sector             String?
  source             String   @default("executive-reporting")
  status             String   @default("completed")
  route              String?
  readinessTier      String?
  authorityType      String?
  canonicalSnapshot  Json
  viewModelSnapshot  Json
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  campaign           AlignmentCampaign? @relation(fields: [campaignId], references: [id])
  artifacts          ExecutiveReportingArtifact[]

  @@index([campaignId])
}

model ExecutiveReportingArtifact {
  id                 String   @id @default(cuid())
  artifactKey        String   @unique
  runId              String
  run                ExecutiveReportingRun @relation(fields: [runId], references: [id], onDelete: Cascade)
  kind               String
  fileName           String?
  mimeType           String?
  payload            Json?
  status             String   @default("authorized")
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([runId])
  @@index([kind])
  @@index([runId, kind])
  @@index([status])
  @@index([createdAt])
}

model DiagnosticJourney {
  id                  String   @id @default(cuid())
  journeyKey          String   @unique
  subjectKey          String
  userId              String?
  email               String?
  organisation        String?
  organisationKey     String?
  diagnosticType      String   @default("diagnostic_journey")
  parentJourneyId     String?
  monitoringCadence   String   @default("ad_hoc")
  status              String   @default("active")
  mergedTensionThread Json?
  escalationHistory   Json?
  routeDecisions      Json?
  startedAt           DateTime @default(now())
  completedAt         DateTime?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  stages              DiagnosticStageRecord[]
  threadSnapshots     DiagnosticThreadSnapshot[]
  monitoringSnapshots MonitoringSnapshot[]
  evidenceNodes       DiagnosticEvidenceNode[]
  decisionObjects     DiagnosticDecisionObject[]

  @@index([subjectKey])
  @@index([userId])
  @@index([email])
  @@index([organisationKey])
  @@index([diagnosticType])
  @@index([parentJourneyId])
  @@index([monitoringCadence])
  @@index([status])
}

model DiagnosticEvidenceNode {
  id           String   @id @default(cuid())
  journeyId    String?
  assessmentId String?
  sessionId    String?
  userId       String?
  email        String?
  sourceStage  String
  kind         String
  label        String
  summary      String
  evidenceText String?
  confidence   Float    @default(0.5)
  severity     String   @default("medium")
  payload      Json?
  createdAt    DateTime @default(now())

  journey      DiagnosticJourney? @relation(fields: [journeyId], references: [id], onDelete: Cascade)

  @@index([journeyId])
  @@index([email])
  @@index([sourceStage])
  @@index([kind])
  @@index([severity])
  @@index([createdAt])
}

model DiagnosticDecisionObject {
  id                String   @id @default(cuid())
  journeyId          String?
  decisionKey        String?
  sessionId          String?
  userId             String?
  email              String?
  sourceStage        String
  decisionText       String
  constraintText     String?
  priorAttemptText   String?
  costOfDelayText    String?
  stakeholderText    String?
  affectedDomain     String?
  normalized         Json?
  confidence         Float    @default(0.5)
  aiExposureLevel       String   @default("MODERATE") // LOW | MODERATE | HIGH | CRITICAL
  aiDisplacementRisk    Boolean  @default(false)
  decisionVelocityScore Int      @default(50)
  forwardTerrainState   String   @default("STABLE") // STABLE | SHIFTING | ACCELERATING | DISRUPTING
  createdAt             DateTime @default(now())
  updatedAt          DateTime @updatedAt

  journey            DiagnosticJourney? @relation(fields: [journeyId], references: [id], onDelete: Cascade)
  strategyLogs       StrategyDecisionLog[]
  retainedDecisions  RetainedDecision[]
  parentDependencies DecisionDependency[] @relation("DecisionDependencyParent")
  childDependencies  DecisionDependency[] @relation("DecisionDependencyChild")
  stakeholders       DecisionStakeholder[]

  @@index([journeyId])
  @@index([email])
  @@index([decisionKey])
  @@index([sourceStage])
  @@index([aiExposureLevel])
  @@index([decisionVelocityScore])
  @@index([createdAt])
}

model DecisionDependency {
  id               String   @id @default(cuid())
  parentDecisionId String
  childDecisionId  String
  relationshipType String   // BLOCKS | ENABLES | AMPLIFIES | CONSTRAINS
  createdAt        DateTime @default(now())

  parentDecision   DiagnosticDecisionObject @relation("DecisionDependencyParent", fields: [parentDecisionId], references: [id], onDelete: Cascade)
  childDecision    DiagnosticDecisionObject @relation("DecisionDependencyChild", fields: [childDecisionId], references: [id], onDelete: Cascade)

  @@unique([parentDecisionId, childDecisionId, relationshipType])
  @@index([parentDecisionId])
  @@index([childDecisionId])
  @@index([relationshipType])
}

model DecisionStakeholder {
  id              String   @id @default(cuid())
  decisionObjectId String
  name            String
  role            String
  function        String
  influenceLevel  String   // LOW | MEDIUM | HIGH | CRITICAL
  alignmentState  String   // ALIGNED | DIVERGENT | BLOCKING | UNKNOWN
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  decisionObject  DiagnosticDecisionObject @relation(fields: [decisionObjectId], references: [id], onDelete: Cascade)
  positions       StakeholderPosition[]

  @@index([decisionObjectId])
  @@index([influenceLevel])
  @@index([alignmentState])
}

model StakeholderPosition {
  id                String   @id @default(cuid())
  stakeholderId     String
  summary           String
  confidence        Float    @default(0.5)
  contradictionFlag Boolean  @default(false)
  createdAt         DateTime @default(now())

  stakeholder       DecisionStakeholder @relation(fields: [stakeholderId], references: [id], onDelete: Cascade)

  @@index([stakeholderId])
  @@index([contradictionFlag])
  @@index([createdAt])
}

model AuditEvent {
  id         String   @id @default(cuid())
  actorType  String   // USER | ADMIN | SYSTEM
  actorId    String?
  objectType String   // DECISION | CONTRACT | ENFORCEMENT_CYCLE | STAKEHOLDER | OUTCOME
  objectId   String
  actionType String   // CREATED | UPDATED | LINKED | BLOCKED | RESOLVED | TERMINATED
  summary    String
  metadata   Json?
  createdAt  DateTime @default(now())

  @@index([actorType])
  @@index([actorId])
  @@index([objectType, objectId])
  @@index([actionType])
  @@index([createdAt])
}

model CaseShareInvite {
  id             String   @id @default(cuid())
  caseId         String
  ownerEmail     String
  recipientEmail String?
  role           String
  tokenHash      String   @unique
  status         String   @default("ACTIVE")
  allowExport    Boolean  @default(false)
  expiresAt      DateTime
  createdAt      DateTime @default(now())
  revokedAt      DateTime?

  @@index([caseId])
  @@index([ownerEmail])
  @@index([status, expiresAt])
  @@map("case_share_invites")
}

model ProvenanceChainAnchor {
  id            String   @id @default(cuid())
  version       Int      @default(1)
  scope         String
  scopeId       String
  anchorType    String   @default("MERKLE_ROOT")
  leafCount     Int
  merkleRoot    String
  previousRoot  String?
  chainHash     String
  computedAt    DateTime @default(now())
  fromTimestamp DateTime?
  toTimestamp   DateTime?
  metadata      Json?
  createdAt     DateTime @default(now())

  @@index([scope, scopeId])
  @@index([scope, scopeId, computedAt])
  @@index([merkleRoot])
  @@index([chainHash])
}

model EnforcementPlaybook {
  id              String   @id @default(cuid())
  name            String
  triggerPattern  String
  actionSequence  Json
  expectedOutcome String?
  status          String   @default("ACTIVE")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  applications    PlaybookApplication[]

  @@index([triggerPattern])
  @@index([status])
}

model PlaybookApplication {
  id                 String   @id @default(cuid())
  playbookId          String
  retainedDecisionId  String
  appliedBy           String?
  status              String   @default("APPLIED")
  outcomeDelta        Float?
  createdAt           DateTime @default(now())

  playbook            EnforcementPlaybook @relation(fields: [playbookId], references: [id])
  retainedDecision    RetainedDecision    @relation(fields: [retainedDecisionId], references: [id], onDelete: Cascade)

  @@index([playbookId])
  @@index([retainedDecisionId])
  @@index([status])
  @@index([createdAt])
}

model FoundationTelemetryEvent {
  id             String   @id @default(cuid())
  organisationId String?
  contractId     String?
  decisionObjectId String?
  eventType      String
  value          Float?
  metadata       Json?
  createdAt      DateTime @default(now())

  @@index([organisationId])
  @@index([contractId])
  @@index([decisionObjectId])
  @@index([eventType])
  @@index([createdAt])
}

model RetainerContract {
  id                   String   @id @default(cuid())
  organisationId       String
  tier                 String   // CORE | OPERATIONAL | INSTITUTIONAL
  status               String   // ACTIVE | PAUSED | TERMINATED
  decisionCapacity     Int
  startDate            DateTime
  endDate              DateTime?
  billingCycle         String   // MONTHLY
  stripeSubscriptionId String?  @unique
  entitlementSlug      String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  organisation         Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  retainedDecisions    RetainedDecision[]

  @@index([organisationId])
  @@index([organisationId, status])
  @@index([tier])
  @@index([status])
  @@index([stripeSubscriptionId])
}

model RetainedDecision {
  id               String   @id @default(cuid())
  contractId       String
  decisionObjectId String
  priorityLevel    String   // HIGH | MEDIUM | LOW
  status           String   // ACTIVE | MONITORED | RESOLVED
  aiLeverageAction String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  contract         RetainerContract         @relation(fields: [contractId], references: [id], onDelete: Cascade)
  decisionObject   DiagnosticDecisionObject @relation(fields: [decisionObjectId], references: [id])
  enforcementCycles EnforcementCycle[]
  playbookApplications PlaybookApplication[]

  @@unique([contractId, decisionObjectId])
  @@index([contractId])
  @@index([decisionObjectId])
  @@index([contractId, status])
  @@index([priorityLevel])
  @@index([status])
}

model EnforcementCycle {
  id                    String   @id @default(cuid())
  retainedDecisionId    String
  cycleDate             DateTime
  actionsTaken          Json
  contradictionsUpdated Json
  outcomeDelta          Float?
  aiDriftDelta          Float    @default(0)
  aiStatusSignal        String   @default("PARITY HOLD") // VELOCITY LOSS | PARITY HOLD | ACCELERATION GAIN
  advantageDelta         Float    @default(0)
  advantageSignal        String   @default("PARITY") // DEFENSIVE | PARITY | ADVANCING | LEADING
  createdAt             DateTime @default(now())

  retainedDecision      RetainedDecision @relation(fields: [retainedDecisionId], references: [id], onDelete: Cascade)

  @@index([retainedDecisionId])
  @@index([cycleDate])
  @@index([retainedDecisionId, cycleDate])
}

model DiagnosticStageRecord {
  id         String   @id @default(cuid())
  journeyId  String
  stage      String
  payload    Json
  createdAt  DateTime @default(now())

  journey    DiagnosticJourney @relation(fields: [journeyId], references: [id], onDelete: Cascade)

  @@index([journeyId])
  @@index([stage])
}

model LongitudinalComparisonRecord {
  id                String   @id @default(cuid())
  journeyId          String
  baselineJourneyId  String?
  subjectKey         String?
  email              String?
  organisationKey    String?
  diagnosticType     String
  cadence            String   @default("ad_hoc")
  deltaSummary       Json
  recurrenceSummary  Json?
  evidenceNodes      Json?
  createdAt          DateTime @default(now())

  @@index([journeyId])
  @@index([baselineJourneyId])
  @@index([subjectKey])
  @@index([email])
  @@index([organisationKey])
  @@index([diagnosticType])
  @@index([createdAt])
}

model MultiStakeholderResult {
  id             String   @id @default(cuid())
  campaignId     String
  organisationId String?
  organisationKey String?
  diagnosticType String
  respondentCount Int
  payload        Json
  evidenceNodes  Json?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([campaignId, diagnosticType])
  @@index([campaignId])
  @@index([organisationId])
  @@index([organisationKey])
  @@index([diagnosticType])
  @@index([createdAt])
}

model OutcomeVerificationRecord {
  id                       String   @id @default(cuid())
  baselineJourneyId         String?
  followUpJourneyId         String?
  decisionObjectId          String?
  sessionId                 String?
  organisationKey           String?
  subjectType               String?
  subjectId                 String?
  outcomeClassification     String
  magnitudeOfChange         Float
  effectivenessScore        Float
  decisionVelocityDelta     Float    @default(0)
  aiCapabilityShift         Float    @default(0)
  competitivePositionShift  Float    @default(0)
  timeToAdvantage           Float    @default(0)   // estimated days to competitive advantage
  unresolvedContradictions  Json?
  payload                   Json
  evidenceNodes             Json?
  createdAt                 DateTime @default(now())

  @@index([baselineJourneyId])
  @@index([followUpJourneyId])
  @@index([decisionObjectId])
  @@index([sessionId])
  @@index([organisationKey])
  @@index([subjectType])
  @@index([subjectId])
  @@index([subjectType, subjectId])
  @@index([outcomeClassification])
  @@index([createdAt])
}

model DiagnosticThreadSnapshot {
  id          String   @id @default(cuid())
  journeyId   String
  snapshot    Json
  createdAt   DateTime @default(now())

  journey     DiagnosticJourney @relation(fields: [journeyId], references: [id], onDelete: Cascade)

  @@index([journeyId])
  @@index([createdAt])
}

model MonitoringSnapshot {
  id             String   @id @default(cuid())
  journeyId      String?
  organisationId String?
  campaignId     String?
  cadence        String   @default("ad_hoc")
  snapshot       Json
  createdAt      DateTime @default(now())

  journey        DiagnosticJourney? @relation(fields: [journeyId], references: [id], onDelete: SetNull)

  @@index([journeyId])
  @@index([organisationId])
  @@index([campaignId])
  @@index([cadence])
  @@index([createdAt])
}

model BenchmarkFact {
  id             String   @id @default(cuid())
  subjectHash    String
  assessmentType String
  dimensions     Json
  metrics        Json
  recordedAt     DateTime @default(now())

  @@index([assessmentType])
  @@index([recordedAt])
}

model BenchmarkCohortSnapshot {
  id         String   @id @default(cuid())
  cohortKey  String
  filters    Json
  sampleSize Int
  metrics    Json
  createdAt  DateTime @default(now())

  @@index([cohortKey])
  @@index([sampleSize])
  @@index([createdAt])
}


model StrategyRoomSession {
  id                 String    @id @default(cuid())
  sessionKey         String    @unique
  status             String
  source             String?
  intake             String?
  canonicalSnapshot  String?
  route              String?
  readinessTier      String?
  authorityType      String?
  lastImpressionAt   DateTime?
  lastFollowupAt     DateTime?
  lastConversionAt   DateTime?
  lastConversionType String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
}

model StrategyRoomRecommendationImpression {
  id                String   @id @default(cuid())
  sessionKey        String
  recommendations   String
  canonicalSnapshot String?
  createdAt         DateTime @default(now())
}

model StrategyRoomFollowup {
  id                     String   @id @default(cuid())
  sessionKey             String
  routeAfter             String
  readinessTierAfter     String
  authorityTypeAfter     String
  clarityDelta           Float
  authorityDelta         Float
  convertedAfterGuidance Boolean
  metadata               String?
  canonicalSnapshot      String?
  createdAt              DateTime @default(now())
}

model StrategyRoomConversion {
  id                String   @id @default(cuid())
  sessionKey        String
  conversionType    String
  metadata          String?
  canonicalSnapshot String?
  createdAt         DateTime @default(now())
}

model AdminDecisionContextualEfficacy {
  id                       String   @id @default(cuid())
  joinKey                  String   @unique
  context                  String
  totalSessions            Int
  impressionCount          Int
  conversionCount          Int
  contextualConversionRate Float
  rankedAssets             String
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
}

// ============================================================================
// CONTENT & KNOWLEDGE MODELS
// ============================================================================

model ContentMetadata {
  id               String      @id @default(cuid())
  slug             String      @unique
  title            String
  contentType      ContentType @default(Briefs)
  summary          String?
  content          String?
  metadata         String?     @default("{}")
  version          String      @default("1.0.0")
  totalPrints      Int         @default(0)
  engagementScore  Float       @default(0.0)
  downloadCount    Int         @default(0)
  viewCount        Int         @default(0)
  fileSize         Int?
  pageCount        Int?
  pdfPath          String?
  createdAt        DateTime    @default(now())
  updatedAt        DateTime    @updatedAt
  lastDownloadedAt DateTime?
  lastViewedAt     DateTime?
  classification   AccessTier  @default(client)

  canonEntries    CanonEntry[]
  sourceRelations ContentRelation[]      @relation("SourceRelations")
  targetRelations ContentRelation[]      @relation("TargetRelations")
  downloadEvents  DownloadAuditEvent[]   @relation("ContentDownloadEvents")
  frameworkAccess FrameworkAccessLog[]   @relation("ContentFrameworkAccess")
  premiumTokens   PremiumDownloadToken[]
  annotations     PrivateAnnotation[]
  dependencies    StrategicLink[]        @relation("SourceBrief")
  dependents      StrategicLink[]        @relation("TargetBrief")

  @@index([slug])
  @@index([classification])
  @@index([contentType])
  @@map("content_metadata")
}

model Framework {
  id               String               @id @default(cuid())
  slug             String               @unique
  title            String
  description      String?
  oneLiner         String?
  tag              String?
  accent           String?
  canonRoot        String?
  audience         String?              @default("[]")
  tier             AccessTier           @default(member)
  pdfPath          String
  fileSize         Int?
  pageCount        Int?
  version          String               @default("1.0.0")
  content          String?
  artifactUrl      String?
  viewCount        Int                  @default(0)
  downloadCount    Int                  @default(0)
  lastViewedAt     DateTime?
  lastDownloadedAt DateTime?
  isActive         Boolean              @default(true)
  metadata         String?              @default("{}")
  createdAt        DateTime             @default(now())
  updatedAt        DateTime             @updatedAt
  downloadEvents   DownloadAuditEvent[] @relation("FrameworkDownloadEvents")
  accessLogs       FrameworkAccessLog[] @relation("FrameworkAccess")

  @@index([slug])
  @@map("frameworks")
}

model StrategicLink {
  id          String          @id @default(cuid())
  sourceId    String
  targetId    String
  strength    Float           @default(1.0)
  linkType    LinkType        @default(DEPENDENCY)
  createdAt   DateTime        @default(now())
  sourceBrief ContentMetadata @relation("SourceBrief", fields: [sourceId], references: [id], onDelete: Cascade)
  targetBrief ContentMetadata @relation("TargetBrief", fields: [targetId], references: [id], onDelete: Cascade)

  @@unique([sourceId, targetId])
  @@map("strategic_links")
}

model ContentRelation {
  id           String          @id @default(cuid())
  sourceId     String
  targetId     String
  relationType String
  weight       Float           @default(1.0)
  createdAt    DateTime        @default(now())
  source       ContentMetadata @relation("SourceRelations", fields: [sourceId], references: [id], onDelete: Cascade)
  target       ContentMetadata @relation("TargetRelations", fields: [targetId], references: [id], onDelete: Cascade)

  @@unique([sourceId, targetId, relationType])
  @@map("content_relations")
}

model PrivateAnnotation {
  id        String             @id @default(cuid())
  body      String
  priority  AnnotationPriority @default(ROUTINE)
  contentId String
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt
  memberId  String
  content   ContentMetadata    @relation(fields: [contentId], references: [id], onDelete: Cascade)
  member    InnerCircleMember  @relation(fields: [memberId], references: [id], onDelete: Cascade)

  @@index([memberId])
  @@map("private_annotations")
}

model CanonEntry {
  id          String           @id @default(cuid())
  slug        String           @unique
  title       String
  description String?
  contentType String
  readTime    Int?
  metadataId  String?
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
  tier        AccessTier       @default(member)
  metadata    ContentMetadata? @relation(fields: [metadataId], references: [id])

  @@map("canon_entries")
}

model StrategicFramework {
  id          String     @id @default(cuid())
  key         String     @unique
  slug        String     @unique
  title       String
  oneLiner    String
  tag         String
  accent      String
  canonRoot   String
  audience    String     @default("[]")
  content     String?
  artifactUrl String?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  tier        AccessTier @default(member)

  @@index([slug])
  @@map("strategic_frameworks")
}

model StrategicIntervention {
  id             String    @id @default(cuid())
  organisationId String
  campaignId     String?
  domain         String
  baselineScore  Int
  status         String    @default("PENDING")
  deployedAt     DateTime?
  createdAt      DateTime  @default(now())

  organisation    Organisation       @relation(fields: [organisationId], references: [id])
  campaign        AlignmentCampaign? @relation(fields: [campaignId], references: [id])
  correctionNodes CorrectionNode[]

  @@index([organisationId])
  @@index([campaignId])
  @@index([status])
}

model CorrectionNode {
  id             String   @id @default(cuid())
  interventionId String
  campaignId     String?
  title          String
  description    String
  priority       String   @default("MEDIUM")
  status         String   @default("OPEN")
  createdAt      DateTime @default(now())

  intervention StrategicIntervention @relation(fields: [interventionId], references: [id])
  campaign     AlignmentCampaign?    @relation(fields: [campaignId], references: [id])

  @@index([interventionId])
}

model DecisionAssetGovernanceRule {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assetId      String?
  assetKind    String?
  ruleType     String
  contextType  String?
  contextValue String?

  minConfidenceScore  Float?
  minContextualWeight Float?
  maxContextualWeight Float?
  minImpressions      Int?
  suppress            Boolean @default(false)
  priorityPenalty     Float   @default(0)
  priorityBoost       Float   @default(0)

  rationale String?
  metadata  String?

  @@index([assetId])
  @@index([assetKind])
  @@index([contextType, contextValue])
  @@index([ruleType])
}

// ============================================================================
// DOWNLOAD & ASSET MODELS
// ============================================================================

model DownloadAuditEvent {
  id                String               @id @default(cuid())
  slug              String
  ipAddress         String?
  userAgent         String?
  createdAt         DateTime             @default(now())
  emailHash         String?
  ipHash            String?
  latencyMs         Int
  metadata          String?              @default("{}")
  processedAt       DateTime
  success           Boolean
  memberId          String?
  errorCode         String?
  errorDetail       String?
  referrer          String?
  statusCode        Int?
  eventType         DownloadEventType    @default(PREVIEW)
  contentId         String?
  deliveredChecksum String?
  deliveryMode      DownloadDeliveryMode @default(DIRECT)
  email             String?
  fileHash          String?
  fileName          String?
  fileSize          BigInt?
  frameworkId       String?
  printAssetId      String?
  requestId         String?
  sessionId         String?
  sourceChecksum    String?
  title             String?
  watermarkId       String?
  contentType       DownloadContentType
  content           ContentMetadata?     @relation("ContentDownloadEvents", fields: [contentId], references: [id])
  framework         Framework?           @relation("FrameworkDownloadEvents", fields: [frameworkId], references: [id])
  member            InnerCircleMember?   @relation("MemberDownloadEvents", fields: [memberId], references: [id])
  printAsset        PrintAsset?          @relation("PrintDownloadEvents", fields: [printAssetId], references: [id])

  @@index([slug])
  @@index([createdAt])
  @@map("download_audit_events")
}

model PrintAsset {
  id          String               @id @default(cuid())
  slug        String               @unique
  title       String
  description String?
  filePath    String
  fileSize    Int?
  fileFormat  String?
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt
  tier        AccessTier           @default(member)
  downloads   DownloadAuditEvent[] @relation("PrintDownloadEvents")

  @@index([slug])
  @@map("print_assets")
}

model PremiumDownloadToken {
  id           String                   @id @default(cuid())
  tokenId      String                   @unique
  contentId    String
  userId       String?
  sessionId    String?
  tier         String?
  issuedAt     DateTime                 @default(now())
  expiresAt    DateTime
  maxDownloads Int                      @default(1)
  usedCount    Int                      @default(0)
  revokedAt    DateTime?
  lastUsedAt   DateTime?
  metadata     String?
  createdAt    DateTime                 @default(now())
  updatedAt    DateTime                 @updatedAt
  attempts     PremiumDownloadAttempt[]
  content      ContentMetadata          @relation(fields: [contentId], references: [slug], onDelete: Cascade)
  user         InnerCircleMember?       @relation(fields: [userId], references: [id])

  @@index([tokenId])
  @@map("premium_download_tokens")
}

model PremiumDownloadAttempt {
  id                 String                @id @default(cuid())
  tokenId            String?
  contentId          String
  userId             String?
  sessionId          String?
  ipAddress          String?
  ipHash             String?
  userAgent          String?
  referrer           String?
  method             String?
  success            Boolean
  statusCode         Int
  reason             String?
  watermarkId        String?
  sourceChecksum     String?
  deliveredChecksum  String?
  createdAt          DateTime              @default(now())
  downloadDuration   Int?
  fileSize           Int?
  pageCount          Int?
  requiredTier       String?
  userTier           String?
  requestFingerprint String?
  token              PremiumDownloadToken? @relation(fields: [tokenId], references: [tokenId])

  @@index([createdAt])
  @@map("premium_download_attempts")
}

model FrameworkAccessLog {
  id           String             @id @default(cuid())
  slug         String
  memberId     String?
  accessType   AccessType         @default(VIEW)
  decision     String
  allowed      Boolean
  requiredTier AccessTier?
  currentTier  AccessTier?
  ipAddress    String?
  userAgent    String?
  createdAt    DateTime           @default(now())
  contentId    String?
  frameworkId  String?
  requestId    String?
  sessionId    String?
  content      ContentMetadata?   @relation("ContentFrameworkAccess", fields: [contentId], references: [id])
  framework    Framework?         @relation("FrameworkAccess", fields: [frameworkId], references: [id])
  member       InnerCircleMember? @relation(fields: [memberId], references: [id])

  @@index([slug])
  @@map("framework_access_logs")
}

// ============================================================================
// DECISION JOURNEY INSTRUMENTATION (Brief C)
// ============================================================================

model DecisionJourneyEvent {
  id        String   @id @default(cuid())
  sessionId String
  userId    String?
  stage     String
  context   String?  // JSON: bundleId, entryPath, buyerState, evidenceDepth, escalationLevel
  createdAt DateTime @default(now())

  @@index([sessionId])
  @@index([stage])
  @@index([createdAt])
  @@map("decision_journey_events")
}

// ============================================================================
// STRATEGY ROOM EXECUTION (Brief D)
// ============================================================================

model StrategyRoomExecutionSession {
  id                    String   @id @default(cuid())
  sessionKey            String   @unique
  strategyRoomSessionId String?
  userId                String?
  email                 String?

  // Entry state
  directive        String?  // allow | warn | restrict | block
  escalationLevel  String?
  conditionSummary String?

  // Decision frame
  coreProblem      String?
  decisionQuestion String?
  constraints      String?  // JSON array
  exposureLevel    String?

  // Intervention stack
  interventionStack String?  // JSON: InterventionStep[]

  // Constraint map
  constraintMap String?  // JSON: authority gaps, resource constraints, stakeholder resistance

  // Canonical snapshot from Strategy Room
  canonicalSnapshot String?

  status    String   @default("active")  // active | completed | escalated | monitoring
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  decisions StrategyDecisionLog[]

  @@index([email])
  @@index([status])
  @@map("strategy_room_execution_sessions")
}

model StrategyDecisionLog {
  id               String    @id @default(cuid())
  sessionId        String
  decisionObjectId String
  decision         String
  status           String    @default("pending")  // pending | executed | blocked | escalated | failed
  notes            String?
  deadline         DateTime?
  avoidanceCount   Int       @default(0)
  executedAt       DateTime?
  escalatedAt      DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  session        StrategyRoomExecutionSession @relation(fields: [sessionId], references: [id])
  decisionObject DiagnosticDecisionObject     @relation(fields: [decisionObjectId], references: [id])

  @@index([sessionId])
  @@index([status])
  @@index([decisionObjectId])
  @@map("strategy_decision_logs")
}

// ============================================================================
// COMMERCIAL RECOVERY
// ============================================================================

model FailedEntitlementGrant {
  id        String   @id @default(cuid())
  email     String
  slug      String
  source    String
  error     String
  resolved  Boolean  @default(false)
  resolvedAt DateTime?
  resolvedBy String?
  createdAt DateTime @default(now())

  @@index([email])
  @@index([resolved])
  @@map("_failed_entitlement_grants")
}

// ============================================================================
// DURABLE ENFORCEMENT (Strategy Room)
// ============================================================================

model ConsequenceTimeline {
  id        String   @id @default(cuid())
  sessionId String
  score     Int
  label     String
  trend     String   // STABLE | ESCALATING | CRITICAL
  baseRisk  Int      @default(0)
  timePenalty Int    @default(0)
  failurePenalty Int @default(0)
  explanation String?
  createdAt DateTime @default(now())

  @@index([sessionId])
  @@index([createdAt])
  @@map("consequence_timeline")
}

model EscalationEvent {
  id          String   @id @default(cuid())
  sessionId   String
  triggerType String   // DEADLINE_EXCEEDED | ACTIONS_SKIPPED | BLOCKED_WITHOUT_REASON | REPEATED_AVOIDANCE
  message     String
  decisionId  String?
  resolvedAt  DateTime?
  createdAt   DateTime @default(now())

  @@index([sessionId])
  @@index([createdAt])
  @@map("escalation_events")
}

// ============================================================================
// CALIBRATION LOOP (Prediction → Outcome → Calibration)
// ============================================================================

model CalibrationState {
  id               String    @id @default(cuid())
  modelKey         String
  modelVersion     String
  status           String    @default("ACTIVE") // ACTIVE | SHADOW | RETIRED
  calibrationData  Json
  outcomeCount     Int       @default(0)
  accuracyScore    Float?
  biasScore        Float?
  lastCalibratedAt DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt

  @@unique([modelKey, modelVersion])
  @@index([modelKey])
  @@index([status])
  @@map("calibration_states")
}

model CalibrationEvent {
  id                 String   @id @default(cuid())
  sessionKey         String
  modelKey           String
  modelVersion       String
  predictionSnapshot Json
  outcomeSnapshot    Json
  predictionError    Float?
  adjustmentProposal Json?
  applied            Boolean  @default(false)
  createdAt          DateTime @default(now())

  @@index([modelKey, modelVersion])
  @@index([sessionKey])
  @@index([applied])
  @@map("calibration_events")
}

model PatternBreakerContract {
  id                    String   @id @default(cuid())
  source                String
  sourceId              String?
  ownerName             String?
  ownerEmail            String?
  commitment            String
  avoidedPattern        String?
  consequenceOfInaction String?
  canonSignals          Json?
  canonDefinitions      Json?
  dueAt                 DateTime
  checkpoints           Json?
  status                String   @default("active")
  verificationStatus    String   @default("pending")
  breachCount           Int      @default(0)
  escalationLevel       String   @default("none")
  metadata              Json?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([ownerEmail])
  @@index([source])
  @@index([status])
  @@index([dueAt])
  @@map("pattern_breaker_contracts")
}

// ─────────────────────────────────────────────────────────────────────────────
// RATE LIMITING — Postgres-authoritative, Redis-optional
// ─────────────────────────────────────────────────────────────────────────────

model RateLimitBucket {
  id          String   @id @default(cuid())
  routeKey    String
  identityKey String
  count       Int      @default(0)
  windowStart DateTime
  expiresAt   DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([routeKey, identityKey])
  @@index([expiresAt])
  @@map("rate_limit_buckets")
}

// ─────────────────────────────────────────────────────────────────────────────
// DECISION MEMORY — public-safe summaries only, no internal scoring data
// ─────────────────────────────────────────────────────────────────────────────

model DecisionMemory {
  id              String   @id @default(cuid())
  userId          String?
  organisationId  String?
  sessionId       String?
  source          String
  state           String
  headline        String
  summary         String
  directive       String
  recommendations Json
  publicSignals   Json?
  escalationLabel String?
  escalationLevel String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([userId])
  @@index([organisationId])
  @@index([sessionId])
  @@index([createdAt])
  @@map("decision_memories")
}

// ─────────────────────────────────────────────────────────────────────────────
// IP ABUSE WATCHDOG — behavioural abuse detection and evidence persistence
// ─────────────────────────────────────────────────────────────────────────────

model AbuseEvent {
  id            String   @id @default(cuid())
  identityKey   String
  ipAddress     String?
  sessionId     String?
  route         String
  ruleTriggered String
  severity      Int      @default(0)
  metadata      Json?
  createdAt     DateTime @default(now())

  @@index([identityKey])
  @@index([ipAddress])
  @@index([createdAt])
  @@index([ruleTriggered])
  @@map("abuse_events")
}

model AbuseFingerprint {
  id              String   @id @default(cuid())
  identityKey     String   @unique
  ipAddresses     Json
  sessionIds      Json
  requestCount    Int      @default(0)
  distinctInputs  Int      @default(0)
  variationScore  Float    @default(0)
  lastSeenAt      DateTime
  firstSeenAt     DateTime @default(now())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([identityKey])
  @@index([variationScore])
  @@map("abuse_fingerprints")
}

model AbuseDecision {
  id            String   @id @default(cuid())
  identityKey   String
  level         Int      @default(0)
  action        String
  reason        String
  expiresAt     DateTime?
  createdAt     DateTime @default(now())

  @@index([identityKey])
  @@index([level])
  @@index([expiresAt])
  @@map("abuse_decisions")
}

model BlockedIdentity {
  id            String   @id @default(cuid())
  identityKey   String   @unique
  ipAddress     String?
  reason        String
  permanent     Boolean  @default(false)
  evidenceCount Int      @default(0)
  blockedAt     DateTime @default(now())
  expiresAt     DateTime?

  @@index([identityKey])
  @@index([expiresAt])
  @@map("blocked_identities")
}

model CanaryTripwire {
  id            String   @id @default(cuid())
  identityKey   String
  ipAddress     String?
  tripwireType  String
  tripwireId    String
  metadata      Json?
  createdAt     DateTime @default(now())

  @@index([identityKey])
  @@index([tripwireType])
  @@index([createdAt])
  @@map("canary_tripwires")
}

model EvidenceVaultEntry {
  id            String   @id @default(cuid())
  identityKey   String
  eventType     String
  severity      Int      @default(0)
  snapshot      Json
  ipAddress     String?
  userAgent     String?
  route         String?
  immutable     Boolean  @default(true)
  createdAt     DateTime @default(now())

  @@index([identityKey])
  @@index([eventType])
  @@index([severity])
  @@index([createdAt])
  @@map("evidence_vault")
}

// ─── Decision State Orchestrator — contact suppression ledger ────────────────

model DecisionContactLedger {
  id        String   @id @default(cuid())
  userId    String?
  sessionId String?
  journeyId String?
  state     String
  system    String
  channel   String
  severity  String
  sentAt    DateTime @default(now())
  metadata  Json?

  @@index([userId])
  @@index([sessionId])
  @@index([journeyId])
  @@index([state])
  @@index([sentAt])
  @@map("decision_contact_ledger")
}

// ─── Privacy Architecture — Data Separation Layer ────────────────────────────

/// Stores decision data WITHOUT any PII. Identified only by opaque sessionId.
model DecisionSession {
  id          String   @id @default(cuid())
  sessionId   String   @unique
  anchors     Json?
  pattern     String?
  trajectory  String?
  source      String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  link        SessionLink?

  @@index([sessionId])
  @@index([createdAt])
  @@map("decision_session")
}

/// Stores user identity with encrypted email. No decision data here.
model UserIdentity {
  id             String   @id @default(cuid())
  emailEncrypted String
  emailHash      String   @unique
  unsubscribed   Boolean  @default(false)
  deletedAt      DateTime?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  links          SessionLink[]

  @@index([emailHash])
  @@map("user_identity")
}

/// Join table — the ONLY bridge between identity and decision data.
model SessionLink {
  id        String   @id @default(cuid())
  sessionId String   @unique
  userId    String
  createdAt DateTime @default(now())

  session   DecisionSession @relation(fields: [sessionId], references: [sessionId])
  user      UserIdentity    @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([sessionId])
  @@map("session_link")
}

// ============================================================================
// WEBHOOK IDEMPOTENCY
// ============================================================================

model ProcessedWebhookEvent {
  id        String   @id // Stripe event ID — guarantees exactly-once processing
  createdAt DateTime @default(now())

  @@map("processed_webhook_events")
}

// ============================================================================
// SOVEREIGN INTELLIGENCE LAYER
// ============================================================================

/// Anonymised diagnostic session record for the Intelligence Commons.
/// The sessionHash is a one-way SHA-256 of (orgId + sessionId).
/// This model contains NO PII — it cannot be used to re-identify any client.
model IntelligenceCommonsRecord {
  id               String   @id @default(cuid())
  sessionHash      String   @unique
  industryTag      String   @default("unspecified")
  revenueBand      String
  teamSizeBand     String   @default("SMALL")
  founderLed       Boolean  @default(false)
  sessionNumber    Int      @default(1)

  // Normalised scores (0–100)
  authorityClarity      Float
  narrativeCoherence    Float
  interventionReadiness Float
  executionReadiness    Float
  overallReadiness      Float  @default(50)

  overallPosture    String   // SOVEREIGN | ALIGNED | DRIFTING | MISALIGNED | DISORDERED
  trajectory        String   // IMPROVING | STABLE | DETERIORATING | COLLAPSING
  failureModeCount  Int      @default(0)

  // Active intelligence signal IDs (JSON array)
  activeSignalIds   Json     @default("[]")

  // Outcome data — populated on follow-up or admin input
  outcomeTag        String?
  outcomeTimeDays   Int?

  recordedAt        DateTime @default(now())

  @@index([revenueBand])
  @@index([industryTag])
  @@index([overallPosture])
  @@index([trajectory])
  @@index([recordedAt])
  @@map("intelligence_commons_record")
}

/// Institutional memory snapshot — one record per completed diagnostic session
/// for a given organisation handle (opaque, not PII).
model InstitutionalMemorySnapshot {
  id                    String   @id @default(cuid())
  organisationHandle    String
  sessionId             String
  sessionNumber         Int

  posture               String
  trajectory            String

  // Scores as individual columns for query efficiency
  authorityClarity      Float
  narrativeCoherence    Float
  interventionReadiness Float
  executionReadiness    Float
  overallReadiness      Float

  failureModeCount      Int      @default(0)
  activeSignalIds       Json     @default("[]")

  revenueBand           String?
  orgState              String?

  recordedAt            DateTime @default(now())

  @@index([organisationHandle])
  @@index([recordedAt])
  @@map("institutional_memory_snapshot")
}

/// Cohort assignment cache — avoids recomputing cohort on every session load.
model CohortAssignment {
  id                 String   @id @default(cuid())
  organisationHandle String   @unique
  cohortId           String
  matchStrength      String   // EXACT | CLOSE | APPROXIMATE
  assignedAt         DateTime @default(now())
  expiresAt          DateTime

  @@index([organisationHandle])
  @@index([cohortId])
  @@map("cohort_assignment")
}

// ============================================================================
// BENCHMARK AGGREGATE CACHE
// ============================================================================

/// Materialised benchmark aggregate — write-through cache updated by the
/// benchmark-context API route when stale (>1 hour). NOT the source of truth.
/// Source of truth: AuditEvent (objectType=OUTCOME_CONTRIBUTION, actionType=CONTRIBUTED).
///
/// key = assessmentKind value, or "__ALL__" for the global (unfiltered) aggregate.
/// One row per key. Upserted on each request that finds a stale or missing row.
model BenchmarkAggregate {
  id                        String   @id @default(cuid())
  /// assessmentKind value, or "__ALL__" for the global aggregate
  key                       String   @unique
  assessmentKind            String?
  n                         Int      @default(0)
  improved                  Int      @default(0)
  resolved                  Int      @default(0)
  unchanged                 Int      @default(0)
  worsened                  Int      @default(0)
  abandoned                 Int      @default(0)
  timeImmediate             Int      @default(0)
  timeShort                 Int      @default(0)
  timeMedium                Int      @default(0)
  timeLong                  Int      @default(0)
  timeDidNotAct             Int      @default(0)
  findingAccurateTotal      Int      @default(0)
  findingAccurateTrue       Int      @default(0)
  recommendationUsefulTotal Int      @default(0)
  recommendationUsefulTrue  Int      @default(0)
  computedAt                DateTime @default(now())
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt

  @@index([assessmentKind])
  @@index([computedAt])
  @@map("benchmark_aggregates")
}

// ============================================================================
// RATE LIMIT EVENTS — Postgres-authoritative, scope-keyed, identifier-hashed
// ============================================================================

/// Scoped rate-limit counter for governance endpoints.
/// Identifier is HMAC-hashed before storage; raw IP/email is never persisted.
/// Compound unique enforces one row per (scope, identifierHash, windowStart).
/// Cleanup job: DELETE WHERE windowStart < now() - interval '14 days'.
model RateLimitEvent {
  id             String   @id @default(cuid())
  scope          String
  identifierHash String
  windowStart    DateTime
  windowSeconds  Int
  count          Int      @default(1)
  limit          Int
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([scope, identifierHash, windowStart, windowSeconds], name: "scope_identifier_window")
  @@index([windowStart], name: "rate_limit_event_cleanup_idx")
  @@map("rate_limit_events")
}

// ============================================================================
// TERMS ACCEPTANCE — versioned, first-authenticated-use tracking
// ============================================================================

/// Records which version of Terms and Privacy a user has accepted.
/// One row per (userId, docType). Upserted on each new acceptance.
/// docType: "TERMS" | "PRIVACY"
model TermsAcceptance {
  id          String   @id @default(cuid())
  userId      String
  email       String
  docType     String
  version     String
  acceptedAt  DateTime @default(now())
  ipHash      String?
  userAgent   String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([userId, docType], name: "terms_acceptance_user_doc")
  @@index([userId])
  @@index([email])
  @@index([docType, version])
  @@map("terms_acceptances")
}

// ============================================================================
// STALE CASE NOTIFICATIONS — duplicate-send protection
// ============================================================================

/// Tracks sent stale-case alert emails per (caseId, band) window.
/// Prevents duplicate sends within the same notification period.
/// muted: user has requested no further alerts for this case.
/// extendedUntil: user has snoozed alerts until this date.
model StaleCaseNotification {
  id            String    @id @default(cuid())
  caseId        String
  userEmail     String
  band          String
  sentAt        DateTime  @default(now())
  returnLinkToken String?
  muted         Boolean   @default(false)
  extendedUntil DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@unique([caseId, userEmail, band], name: "stale_case_notification_window")
  @@index([caseId])
  @@index([userEmail])
  @@index([sentAt])
  @@map("stale_case_notifications")
}

#!/usr/bin/env node
// No-op codemod to keep fix:tw-all green.
// Replace with real logic if you need classnames(...) slash-opacity fixes.

/* eslint-disable no-console */
console.log("[classnames] no changes needed (noop)");
process.exit(0);
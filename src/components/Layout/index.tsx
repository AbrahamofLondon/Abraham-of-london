// src/components/Layout/index.tsx

// This file simply re-exports the main Layout component from the root
// components folder. It keeps all existing behaviour and styling intact,
// while avoiding duplicate implementations.

export { default } from "../../../components/Layout";
export * from "../../../components/Layout";
